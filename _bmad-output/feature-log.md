# Feature Log — google-ads-agent (BMAD drift reconcile, Tier 1)

Lightweight delta capture so the BMAD planning artifacts don't silently drift from the code.

- **Tier 1 (this file):** every session that ships a feature appends ONE row to the Active table below — *before ending the session*. Cheap (~10s), captures the *why* while it's fresh. Same reflex as the chronicle line in this folder's product-dev memory.
- **Tier 2 (reconcile):** when this log exceeds ~10 unreconciled rows **OR** a month has passed **OR** before a version milestone, run `/bmad-document-project` to fold these deltas back into `_bmad-output/planning-artifacts/`. Then move the folded rows under **Reconciled** with the date.

Convention: **Story** = the epic/story id it maps to, or `NEW — unplanned` if it had no story. Keep each row to one line.

## Active (unreconciled)

| Date | Feature / change | Story | Files touched |
|---|---|---|---|
| 2026-07-16 | **`primary_for_goal` write on conversion action tools (closes confirmed MCP gap).** The in-app agent hit this live: it needed to demote a deprecated conversion action from primary to secondary, but `conversion_update_conversion_action` only exposed name/status/counting_type — zero `primary_for_goal` occurrences existed under `google_ads/services/`. Extended `ConversionService.update_conversion_action` (service method + MCP tool wrapper) with optional `primary_for_goal: bool \| None`; when provided it sets the field AND appends `"primary_for_goal"` to the FieldMask (mirrors the WS1 `update_ad_final_urls` field-mask precedent) — `None` leaves the mask untouched (back-compat). Also added `primary_for_goal: bool \| None` to `create_conversion_action` (method + wrapper), set only when not-None so the API default is preserved (`category`/`counting_type` were already exposed). Harness: the existing `conversion_update_conversion_action` entry in `validate_all_tools.py` now passes `primary_for_goal: False` so the demote mask path runs fail-closed under `validate_only`. Tests: new `test_conversion_primary_for_goal.py` (5 tests) — demote builds `["primary_for_goal"]` mask, omitted leaves mask unchanged, `True` promotes symmetrically, create sets-only-when-provided, and `enumerate_tools` discovers the tool + surfaces the param in both update & create schemas. Suite 284→289 green. No live mutations (mocks only). **NEEDS MANUAL RESTART** — `.py` change; `launchctl kickstart -k gui/$(id -u)/<backend-label>` to reload the MCP. | NEW — unplanned (MCP conversion-action write gap) | backend/google_ads/services/conversions/conversion_service.py, backend/validate_all_tools.py, backend/tests/test_conversion_primary_for_goal.py |
| 2026-07-16 | **Stop-disposition safety warning (UI half of the P0):** the ledger now renders the backend's `turn_stopped` disposition payload instead of dropping it in the reducer `default`. New `TurnStoppedPayload.specialists[]` type (`disposition: 'completed' \| 'stopped_before_write'`); `buildModel` adds a `case 'turn_stopped'` that pushes a new `stop_warning` row ONLY when a specialist is `stopped_before_write` (empty/all-completed = no false alarm). Row renders a danger-alert box (AlertTriangle + `border-danger/40 bg-danger-soft text-danger`) reading "Approved write may not have executed. Verify before assuming it applied." plus one quiet line per affected specialist ("{role_name} stopped before its write completed"). Added to `rowSignature` for the memo; and surfaced above the collapsed summary so it stays visible even when the stopped turn completes collapsed. Closes the silent-stopped-write gap. | NEW — unplanned (P0 stop-disposition safety, UI half) | frontend/src/components/chat/OrchestrationLedger.tsx, frontend/src/types/orchestration.ts |
| 2026-07-16 | **`generate_keyword_ideas` read tool for the cmo-agent:** new read-only method + tool on `SearchService` (metadata) wrapping `KeywordPlanIdeaService.GenerateKeywordIdeas` — keyword research (ideas + avg_monthly_searches + competition + low/high top-of-page bid micros) seeded by `keywords` and/or `page_url`, `geo_target` (default "2124" Canada), `language` (default "1000" English), `limit` (default 50). Bare geo/language ids expanded to `geoTargetConstants/<id>` / `languageConstants/<id>`; picks Keyword/Url/KeywordAndUrl seed by args; response iterator capped at `limit`. GenerateKeywordIdeas MUTATES NOTHING → safe on the read surface. Mounted via the existing `search_server` (`--groups reporting`, namespace `search`) so it serves as wire name `search_generate_keyword_ideas` — CMO's 6th read tool on the google connector. Live-validated on customer 7178239091 (MCC login_customer_id from client config), 50 real rows (e.g. "citizenship by investment" 480/mo MEDIUM; "golden visa portugal" 390/mo MEDIUM). | NEW — unplanned (cmo-agent E3 MCP read-surface expansion) | backend/google_ads/services/metadata/search_service.py |
| 2026-07-16 | **Chat orchestration perf + dup-send fixes:** (2) batched SSE event ingestion — `orchestratedSend` now buffers events in a ref and drains them into `setTurnEvents` on a ~90ms interval (synchronous flush on terminal/`turn_stopped`/`turn_error`/`turn_done` + stream-end + finally) instead of a state write per event, so a ~66-event claim-gate + token-level deltas no longer re-render the ledger per token; identity guards + `final_chunk`→bubble path stay per-event. Memoized ledger rows (`MemoLedgerRow` = `React.memo` over a stable `rowSignature`) so a token delta re-renders ONLY the active specialist's row, not all 300+. Optimistic Stop — new `stopping` state disables + spins the Stop button on click, before the `stopTurn` round-trip. (3) duplicate-send guard — identical trimmed text re-submitted within 10s while in-flight/queued is dropped with a quiet "Already queued" hint (`lastSendRef` + `DEDUP_WINDOW_MS`); different text / same text after 10s still sends. | NEW — unplanned (perf + UX) | frontend/src/components/layout/ChatPanel.tsx, frontend/src/components/chat/OrchestrationLedger.tsx, frontend/src/components/chat/ChatInput.tsx |
| 2026-07-16 | **Two surgical chat-orch safety fixes.** **FIX 1 (P0 stop safety — approved write must never die silently):** `_drive` in chat_runner now tracks every dispatched WRITE-intent specialist by observing `agent_called(+write_intent)`/`agent_result` as they flow through `_emit` (approach (b) — state stays in the runner that owns the terminal event, no cross-boundary handback). Terminal `turn_stopped` payload gains `"specialists":[{role_id,role_name,disposition}]` (`completed`=agent_result seen before stop / `stopped_before_write`=dispatched write never completed), empty list = common safe case; existing `stopped_by`/`partial_persisted` kept. Write-intent per spec computed in S3 PLAN via `_detect_write_intent` (explicit plan `write_intent` bool wins, else verb heuristic over task+tools: push/create/update/add/remove/pause/enable/set/mutate/apply/…) and surfaced on `plan`/`agent_called` events (threaded through `_dispatch_specialist`'s `agent_start`). **FIX 4 (stale conversion registry in VERIFY):** new best-effort `fetch_conversion_actions(account_id)` in agent.py (mirrors `fetch_ad_landing_pages` — NEVER raises, returns `(block, rows)`) + new `GoogleAdsService.get_conversion_actions` (read-only GAQL `... FROM conversion_action WHERE status='ENABLED'`). S2 VERIFY pulls it UNCONDITIONALLY every orchestrated turn (one cheap read, degrades silently), injects the block into `premise_block` under a "supersedes any remembered registry" header (reaches both PLAN + SYNTH Director prompts), tags it into the manifest as LIVE_API (`add_live_api(block, now_iso, tool_name="conversion_action_gaql")`) so the claim gate can verify conversion-action claims, and emits `verification {kind:"conversion_actions", status:"verified"\|"failed"}`. Decisions beyond spec: chose approach (b) for FIX 1 (runner-local, no orchestrator handback); `stopped_before_write` for all incomplete write specialists (honest conservative report — runner can't distinguish "never ran" from "ambiguous mid-flight"); added a `_raising_conv` belt-and-suspenders test since the orchestrator also guards the fetch call. Tests 264→273 (+9: 3 stop-disposition, 3 conversion-injection incl. failure+raising paths, 3 write-intent-detection units). All mocked, no live API, no restart. | NEW — unplanned (P0 stop-safety + VERIFY conversion refresh) | backend/app/services/{chat_runner,chat_orchestrator,agent,google_ads}.py, backend/tests/{test_chat_stop_isolation,test_chat_orchestrator}.py |
| 2026-07-16 | **Three diagnosed chat-orch defects — surgical fixes.** **FIX 1 (turn budget kills mid-synthesis silently):** config gains `CHAT_ORCH_SYNTH_RESERVE_USD=1.0` + `CHAT_ORCH_SYNTH_RESERVE_SEC=45`; the S4 DISPATCH backstop now ring-fences that reserve (degrades at `cap - reserve`, cancels outstanding specialists) so S6 SYNTHESIZE always has room. New VISIBLE `budget_notice` ledger event (structured `{reason,cost,cap_usd,elapsed_s,cap_s,specialists_done,specialists_total}` via `_budget_snapshot`) replaces the old bland director_thought. When the cap is truly blown by S6, the LLM synth is SKIPPED and a deterministic COMPLETE wrap-up is composed from state (`_compose_budget_wrapup` — "Turn budget reached — here's where things stand:" + per-specialist summary + pending list + unresolved-conflict note) so the turn never ends mid-sentence. Cap stays $5/6min. **FIX 2 (meta-questions misrouted → empty answer):** new `_is_meta_question` (regex + terse-exact set) routes "why did you stop?"/"what did you just do?"/"repeat that" to DIRECT even under force_mode='orchestrate' (hard override in S0 TRIAGE) — no recall/verify/plan. And `_run_direct` now streams the DIRECT + DEGRADE answer as `final_start`/`final_chunk`/`final_done` (was `text`, which the orchestrated bubble renders NOTHING for) + an empty-text fallback line, so degrade always yields a visible reply. **FIX 3 (read-only GAQL blocked as "analysis-only"):** `_dispatch_specialist` now grants a read-only google-ads whitelist (`_specialist_tool_allowlist`: `search__execute_query` + search/list/keyword-idea tools) UNION any plan-authorized tools, instead of `tools=[]`→`__NONE__` (which walled every SELECT). Mutates stay blocked by the SAME middleware (not in the whitelist). `execute_query` hardened with a SELECT-only guard. Director synth/plan stages stay tool-less. Tests 273→284 (+11: budget wrap-up + within-budget, meta-route + degrade-renders, specialist whitelist + unit + REAL middleware allow-SELECT/block-mutate, execute_query guard). Frontend: `BudgetNoticePayload` type + `budget_notice` ledger row (amber Hourglass warning, visible through collapse). tsc clean, fresh vite dist. No live API in tests, no restart. | NEW — unplanned (chat-orch P0 fixes: budget wrap-up + meta-triage + read-only whitelist) | backend/app/config.py, backend/app/services/chat_orchestrator.py, backend/google_ads/services/metadata/search_service.py, backend/tests/test_chat_orchestrator.py, frontend/src/types/orchestration.ts, frontend/src/components/chat/OrchestrationLedger.tsx |

_(append new rows to the table above)_

## Reconciled

_Rows folded into the planning artifacts. Format: `YYYY-MM-DD — reconciled via /bmad-document-project (covers <date range>)` followed by the moved rows._

**2026-07-14 — reconciled via Tier-2 fold-back (covers 2026-06-02 → 2026-07-14, 55 rows).** Folded into: `planning-artifacts/prd-v2.md` (Phase 1.5/1.6 shipped statuses + new Phase 1.7 + §11 pointer), `planning-artifacts/epics-v2.md` (Epic 8/9/13 story status notes, Epic-List statuses, § "Shipped Unplanned" ledger, implementation-order statuses), `planning-artifacts/architecture-v2.md` (§11 Shipped Delta Ledger: V18–V24 migrations, services, transport, ops model). Note: 19 of these rows had been parked below this heading by earlier sessions without a marker — this marker covers them too (they appear after the Active-section rows below).

| Date | Feature / change | Story | Files touched |
|---|---|---|---|
| 2026-07-14 | **Chat-orch-v2 Epic 4 — real claim gate + provenance manifest (S7 was a stub):** New `provenance.py` = pure `ProvenanceManifest` (entries `{tag,ts,kind,ids,detail}`, tags LIVE_API/PAGE_FETCH/LOCAL_STORE/MEMORY; `add_page_fetch` parses the landing-page block for urls+form/tracking flags, `add_live_api` harvests IDs from tool_result outputs, `add_local_store`/`add_memory`/`add_from_findings`; shared `ID_PATTERNS`+`extract_ids` reused from agent `_condense_for_memory` regex family — GTM-/AW-/G-/Conversion ID/Label/9+-digit; `verified_ids()` = LIVE_API∪PAGE_FETCH∪fresh-LOCAL_STORE, bare MEMORY excluded). New `claim_gate.py` = deterministic pure `run_claim_gate(final_text, manifest, page_verified)`: (a) unverified/unlabeled ID tokens REWRITTEN in place to `[ID not verified this session — pull it before relying on it]`; (b) material numbers FLAG-only with a **derived-math guard** (`$706 (45% × $1,569)` passes clean — parenthetical-math + adjacent ×/%/=/of window); (c) page-state assertions traced to a PAGE_FETCH entry — REWRITTEN when fetch failed/unbacked, FLAGGED when no fetch ran. Event `{checked, passed, rewritten, flagged}`, passed=checked−rewritten−flagged. Wired into orchestrator: manifest built at ORCHESTRATE top, populated at S1 (recall→memory/local), S2 (page_fetch + page_verified), S4 (tool_result outputs now captured in `_dispatch_specialist` → add_live_api) + add_from_findings; S7 stub replaced — gate runs on final BEFORE persistence, **persisted message = gated text**, real claim_gate event emitted. Frontend: `ClaimGatePayload.flagged?` + `ClaimGateRow` model; `ClaimGateLedgerRow` — clean gate = single quiet row, issues = calm warning-soft "N corrected" chip + expandable rewritten/flagged list (never danger-red, tokens only, DESIGN.md-clean). Tests 238→256 (+18: manifest units, 4 key cases incl. derived-math no-false-positive, §4.4 event shape, +1 run_turn integration proving gated persist). tsc 0→0, dist rebuilt (bundle hash changed). **Backend restart required** (new .py modules) — FLAGGED, not restarted. | E4.1/E4.2/E4.3 | backend/app/services/{provenance,claim_gate}.py (new), backend/app/services/chat_orchestrator.py, backend/tests/test_claim_gate.py (new), frontend/src/types/orchestration.ts, frontend/src/components/chat/OrchestrationLedger.tsx |
| 2026-07-13 | **Studio Redesign MVP — Epic A (the fork):** `/studio` split into a thin StudioRouter (reads useLocation) → StudioHome (three door cards over shared Library/Souls/Presets) + AIVideoStudio + KineticStudio. 4 new routes (`/studio/ai-video`, `/studio/ai-video/:projectId`, `/studio/kinetic`, `/studio/kinetic/:lane`) all `element={<AppRoot/>}` — existing `startsWith('/studio')` bridge already catches them, no bridge change. Door cards are true choice-cards each w/ DISTINCT meta (AI Video: Clapperboard glyph + "12 models · credits" + Resume chip when a drafting/storyboard project exists; Kinetic: "local · free"; Image: "10 models · credits" → opens existing StudioPanel image mode per §2.4 thin-door §13 default). Removed hub "Write script"+"New video" buttons; kept credits pill/local badge/Back/Upload/Create. appStore.setShowStudio untouched (fork lives inside the studio surface). DESIGN.md-clean (no dark/gradient/glassmorphism/icon-grid/em-dash), keyboard-focusable, transform/opacity hover lift. | Studio-Redesign Epic A (A1/A2/A3) | frontend/src/App.tsx, frontend/src/components/studio/{StudioPage(→StudioRouter),StudioHome,AIVideoStudio,KineticStudio}.tsx |
| 2026-07-13 | **Studio Redesign MVP — Backend core (Video Director + persistence + Brand Avatar):** Migration **V23** (plan said V19 — stale; DB was at V22) adds `studio_video_projects` (15 cols, source-of-truth for storyboards) + `brand_avatars` (id/account_id/name/soul_id/voice_id/style_notes/created_at). `model_catalog.py` 12 video entries get additive `origin`+`strengths` (CatalogModel +2 optional fields, /api/studio/models backward-compat). New `video_director` role in ROLES registry (avatar=clapperboard, VO-pacing word budgets, RULE 0, forced-JSON storyboard contract, 3-concept scaffold; per-account `data/roles/{account_id}/video_director.md` override). `services/video_director.py` = `video_director_turn` run_fn riding chat_runner (V0 context→V1 scoped campaign-Director consult via _dispatch_specialist shape, 90s timeout DEGRADE-not-block→V2 _stage1_decompose→V3 3 concept loglines [straight-to-draft §13 default on `angle:` msgs]→V4 model-aware storyboard w/ server-side clamp_duration enum-snap+≤8-scene cap+±1 deviation, persist JSON→V5 append_role_notes). `routers/video_director.py` = video-projects CRUD + `/draft` + brand-avatars CRUD (all `/api/studio` prefix; §6.6 conversation-rebind = new conversation on campaign-link change). `AVATAR_SPEAK_MODEL=None` seam for future lipsync. Cost/render stay OUTSIDE the LLM turn (a stopped draft can never burn credits). Tests 194→216 green (mocked LLM: brief→concepts→storyboard w/ Veo enum-snap + Kling int-cap; consult degrade; avatar CRUD). **Backend restart required** (launchd service, new router). | Studio-Redesign Epic C-core + Brand Avatar (added story) | backend/app/database.py, backend/app/services/{video_director,model_catalog,roles}.py, backend/app/routers/{video_director,studio}.py, backend/app/main.py, backend/tests/test_{studio_video_migration,video_director_role,video_director_turn,video_director_crud}.py |
| 2026-07-13 | **Studio Redesign MVP — Epic B+C4 (AI Video Studio workspace + Video Director dock):** Full 3-zone workspace at `/studio/ai-video` (SETUP rail 264px / STORYBOARD canvas / VIDEO DIRECTOR dock 360px collapsible). Model gallery sheet = 12 video-catalog cards grouped by tier w/ origin/strengths/cost + live clip-math (vs the untouched `<select>` PMax hosts use). Storyboard canvas = per-scene cards (mono duration chip, editable visual/VO, per-scene model override, thumbnail slot), "N/8 scenes" counter, cost gate footer (POST /video-engine/estimate, `N+` partial, Render button carries credit total — never naked). Appendix-B render mapping (per-scene `speak:''` + whole-track `voiceover_script:vo_full`). Dock reuses `streamTurn`/`parseSse`/`fetchActiveTurns`/`stopTurn` (cursor-replay reconnect, no new SSE parser) rendering director_thought/consult-row/concepts(pick→/draft angle)/storyboard(card-by-card populate)/prose/turn_done. Project CRUD wired (create/PATCH-debounced/resume via :projectId, DB=truth refresh-proof). Single `useClipMath` hook (Risk-5). Brand Avatar picker in rail for soul-capable models (§13 default gating). agentProfiles `video_director` entry. Consult ON when linked (§13 default). tsc 33→33, StudioPanel/PMax/ModelPicker-select/appStore/ChatInput untouched. | Studio-Redesign Epic B (B1/B2/B3) + C4 + Brand Avatar | frontend/src/components/studio/{AIVideoStudio,useClipMath}.tsx, frontend/src/components/studio/aivideo/{SetupRail,StoryboardCanvas,DirectorDock,SceneCard,ModelGallerySheet,shared,types}, frontend/src/lib/{api,agentProfiles}.ts |
| 2026-07-13 | **Studio Redesign MVP — Epic D (Kinetic Studio recomposition):** `/studio/kinetic` recomposed from the legacy VideoCreator strip into 3 lanes as top tabs (Brand Reel "Pillow+ffmpeg" / Premium Reel "Hyperframes GSAP" incl. Brand Story two-phase plan→storyboard→render / Presenter "HeyGen" w/ ScriptGenerator folded in as Step 1), two-column BRIEF/PREVIEW layout. Logic extracted into hooks (`useKineticRender` wraps consumeStream + single-shot POSTs; `useBrandStoryPlan` = plan/preview/render; `useKineticLibrary`). Render payloads verified **byte-identical** to legacy VideoCreator (only additive `soul_id` on /generate, serializes to nothing when unset). Brand Avatar picker in Presenter Step 2 (stamps soul_id→render + voice_id→voice select; `/studio#souls` deep-link when none; AVATAR_SPEAK_MODEL seam) — §13 default presenter-lane placement. D3 type-preview panel shipped (mirrors mercan-hero type stack, honest "type preview" label). All banned colors (pink/amber/violet/emerald) → tokens; verbatim-lock uses warning token; storyboard cards restyled to §3.3. **VideoCreator.tsx KEPT** (plan §9.3 "delete" NOT executed — ChatInput.tsx:496 still mounts it; deleting would break the chat video flow); Kinetic no longer imports it. tsc 33→33, vite bundler passes. | Studio-Redesign Epic D (D1/D2/D3) + Brand Avatar picker | frontend/src/components/studio/KineticStudio.tsx, frontend/src/components/studio/kinetic/{useKineticRender,useBrandStoryPlan,useKineticLibrary,KineticShared,TypePreview,BrandReelLane,PremiumReelLane,PresenterLane} |
| 2026-06-02 | App-wide visual re-skin to Shopify "Studio" calm light design (OKLCH token layer + shadcn aliases → light default; `.studio-prose`/`.studio-caret`/`.studio-pulse`/`.label-section`; quiet tool rows; avatar-lane assistant turns; sunken composer well) | NEW — unplanned | frontend/src/index.css, frontend/index.html, frontend/DESIGN.md (new), frontend/PRODUCT.md (new), src/components/chat/{ChatMessage,ChatInput,ContextBadge,MemoryPanel,AgentAvatar,ToolCallBlock}.tsx, src/components/layout/ChatPanel.tsx |
| 2026-06-10 | Default model bumped Opus 4.8 → Fable 5 (`claude-fable-5[1m]`; plain `claude-fable-5` = fallback; opus/sonnet/haiku aliases kept) | NEW — unplanned | backend/app/services/{agent,token_counter,workflow_orchestrator}.py, backend/app/models/schemas.py, frontend/src/components/chat/ChatInput.tsx, components/campaign/CampaignBuilder.tsx, components/layout/ChatPanel.tsx, lib/{chatApi,chatTemplates}.ts, README.md, frontend/PRODUCT.md |
| 2026-06-10 | PRD+epics updated for commercialization: Phase 1.5 added to PRD §8 (PMax finalization P0, MCP plan tools P0, Shopping P1; supersedes "local-first forever" non-goal for the hosted-MCP track) + Epics 8/9/10 added to epic list & implementation order (stories TBD via /bmad-create-epics-and-stories). One-time direct edit authorized by Wassim (BMAD-governance rule otherwise) | Planning artifacts | _bmad-output/planning-artifacts/{prd-v2,epics-v2}.md, research/product-roadmap.md |
| 2026-06-10 | Epic 8 PMax finalization: local-UUID→Google image-asset bridge in orchestrator (pass-through for real resource names, webp/oversize→PNG/JPEG transcode via Pillow, pre-flight 422 for missing files), audience signals attached post-link (search themes + audiences, best-effort warnings), step-aware PMaxStepError + rollback report surfaced to wizard (502 detail incl. step/rolled_back), wizard now sends collected signal hints | Epic 8 | backend/google_ads/services/campaign/pmax_orchestrator.py, backend/app/routers/pmax.py, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-06-10 | Epic 9 MCP plan tools: create_plan/list_plans/approve_plan/skip_plan/run_plan_now on the HTTP MCP bridge (bearer auth; budget/bids/status/geo default approval-gated; reuses scheduler lifecycle; 10 tools total registered). Epics 8-10 stories written into epics-v2.md (9 stories). Autonomous execution authorized by Wassim 2026-06-10 (1-hour window; no live Google Ads mutations run) | Epic 9 + planning | backend/app/mcp_server.py, _bmad-output/planning-artifacts/epics-v2.md |
| 2026-06-10 | Epic 8 PMax live-test fixes: asset group now created ATOMICALLY with all AssetGroupAsset links in one GoogleAdsService.Mutate (temp resource name customers/{cid}/assetGroups/-1; Google validates PMax asset minimums at creation — empty create + later links always rejected with NOT_ENOUGH_*); rollback now uses REMOVE mutate operations for campaign AND budget (update status=REMOVED is rejected with INVALID_ENUM_VALUE; budget removed after its campaign, was previously always orphaned). Verified live: campaign 23934110143 created PAUSED on 7178239091 (attempt 2/5; attempt 1 failed only on DUPLICATE_CAMPAIGN_NAME from a pre-existing orphan and auto-removed its own budget) | Epic 8 | backend/google_ads/services/campaign/pmax_orchestrator.py, backend/google_ads/services/campaign/campaign_service.py, backend/google_ads/services/bidding/budget_service.py |
| 2026-06-10 | PMax wizard UX: "Draft with Creative Director" live (new POST /accounts/{id}/pmax/draft-copy — creative_director role, analysis-only, fetches landing page, server re-enforces ≤30/≤90 char limits; live-tested on Greece GV: 12 headlines/5 long/5 descriptions all grounded + in-limit, ~2.5min) + campaign brief field on step 1 + "why is Next disabled" amber hint on every step (was silent validation gate, read as a bug) | Epic 8 | backend/app/routers/pmax.py, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-06-11 | CLI discovery prefers NATIVE Claude binary (~/.local/bin/claude, 2.1.172) over stale npm cli.js (2.1.72) in every Claude-subprocess spawn: new _find_cli() argv-prefix (native -> [node, cli.js] -> which claude), all spawn sites switched to *argv; verified resolving ['~/.local/bin/claude'] on this Mac | NEW — unplanned | backend/app/services/{agent.py,compaction.py,chronicle.py,guideline_optimizer.py,skill_optimizer.py,brand_reel.py} |
| 2026-06-11 | PMax wizard video step: agent-made slideshow ad + YouTube upload (Story 8.4). Draft job: script_generator writes 20-30s storyboard over operator-picked library images (landing-page grounded, per-scene `_speak_text`, palette validation + round-robin image recovery). Render job: generate_storyboard_reel → ad_assets with account_id (avatar_id=pmax-video). YouTube: one-time OAuth connect (youtube.upload scope, localhost:8000 callback, refresh token → data/youtube_token.json chmod 600 + gitignored), unlisted resumable videos.insert in worker thread, returned id auto-appends to bundle.videoIds. Live-verified draft + render (asset 30f99346, 12.8s, 61s render); upload mock-tested only (no token yet — Wassim must click Connect once) | Epic 8 (Story 8.4) | backend/app/routers/{pmax_video.py,youtube.py} (new), backend/app/services/youtube_uploader.py (new), backend/app/main.py, backend/pyproject.toml, .gitignore, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-06-11 | PMax video step — YouTube metadata generators (Story 8.4 extension): POST /api/pmax/video/metadata job+poll (creative_director, tool_allowlist=[], landing-page grounded → 3 title options ≤95c + 2-3 para description; server truncates, strips em dashes, appends final_url if missing); POST /api/pmax/video/frames (ffmpeg stills at 10/35/60/85% of duration → ad_assets image rows with account_id = thumbnail candidates); /api/youtube/upload now accepts description + thumbnail_asset_id (thumbnails.set after insert, Pillow re-encode to <2MB/1280px JPEG; 403 channel-not-verified degrades to `warning` string, never fails the upload). UI: "Generate title & description" → 3 clickable title chips (first preselected) + editable description textarea; thumbnail row = Pick frame (4 inline stills) / From library (LibraryPicker single-select; onToggle now passes the asset for URL) / Generate (Higgsfield 16:9 modal w/ promptContext); selected-thumb preview + amber upload warning. Live-tested: frames on asset 30f99346 (4× 1920×1080 jpg, account-scoped rows) + metadata draft on Panama QIV (3 grounded titles ~70c, 3-para description w/ CTA+URL, no em dashes). NO YouTube upload run | Epic 8 (Story 8.4 ext) | backend/app/routers/{pmax_video.py,youtube.py}, backend/app/services/youtube_uploader.py, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-06-11 | 3 live-bug fixes: (1) ASPECT_RATIO_NOT_ALLOWED on PMax submit — image bridge now center-crops every local image to the slot's EXACT Google aspect pre-flight (`_fit_image_for_slot` + IMAGE_SLOT_SPECS: logo 1:1/128², landscape 1.91:1/600×314, square 1:1/300², portrait 4:5/480×600, ±1% tol), downscales >5MB at-aspect, rejects below-min with clean 422 listing image+slot; compliant files pass through raw (keeps GIFs); google-resource refs untouched; wizard ImageGroup/LibraryPicker copy now says "auto-cropped at submit". Replaces `_transcode_for_google`. Unit-tested w/ synthetic Pillow images (16:9→1.91:1 crop, 1024² noop, tiny→422). (2) Signals "Add another" dead — onChange filtered empties so the appended '' row vanished; now stores raw rows, trims/filters at submit+review. (3) Variant cards: prompt/rationale select-text + per-card copy-to-clipboard icon (Check feedback, execCommand fallback) | NEW — unplanned (Epic 8 live bugs) | backend/google_ads/services/campaign/pmax_orchestrator.py, frontend/src/components/campaign/PMaxWizard.tsx, frontend/src/components/studio/HiggsfieldGenerator.tsx |
| 2026-06-11 | 2 live-bug fixes: (1) ASPECT_RATIO_NOT_ALLOWED STILL on resubmit (idx 23 MARKETING_IMAGE) — root cause: step-3c upload dedupe keyed by local UUID alone, so one image in two different-aspect slots uploaded ONCE with the FIRST slot's crop (1:1 logo crop linked as 1.91:1 MARKETING_IMAGE); now keyed (uuid, slot-aspect). Plus: kind=google pass-throughs (resource names/numeric ids, e.g. poisoned resubmit bundles) now GAQL-verified pre-flight (`_fetch_image_asset_dims` — image full_size WxH; off-aspect or unverifiable → 422 naming slot+asset, no Google entities created); wizard sanitizes restored sessionStorage bundle (drops resource-name/numeric refs from image slots so server's UUID→resource swap can never persist client-side). Regression unit tests: backend/tests/test_pmax_resubmit.py (stdlib unittest + Pillow synthetics + stubbed services — cross-slot crop-per-aspect + resubmit re-upload, off-aspect 422, unverifiable 422). (2) YouTube panel not refresh-proof — ytTitle/ytDescription/titleOptions/thumb/uploadedId moved into the persisted VideoPanelState (pmax-video-state sessionStorage, merge-over-defaults restore) | NEW — unplanned (Epic 8 live bugs) | backend/google_ads/services/campaign/pmax_orchestrator.py, backend/tests/{__init__.py,test_pmax_resubmit.py} (new), frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-06-11 | Studio redesign PHASE A (per research/studio-redesign-brief.md APPROVED + video-engine-plan P1): (1) GET /api/studio/models — server-side model catalog (services/model_catalog.py, curated static + per-model constraints encoding Veo duration string-enum vs Kling int vs no-duration, plain tiers Best quality/Fast/Budget, cost text; liveness via cached 1h `higgsfield model list` through new HiggsfieldClient.model_list; never 502s — falls back to static). (2) StudioPanel.tsx — shared 480px slide-over, modes image|video|copy, `context` prop is the ONLY google-ads coupling (decoupling addendum honored: zero google_ads imports in studio service layer); intent input + Enhance (Visual Director context-mode extract-brief, 3-angle picker), streaming results grid via extracted useStudioJobs hook (SSE + poll fallback + per-item retry via watchMore), quiet bottom controls (ModelPicker.tsx fed by catalog, live cost, aspect locked from context, one Tune disclosure: variants/duration-snapped-to-contract/kling mode/veo quality/soul), empty-credits top-up link, stays mounted so jobs survive close/reopen, "Use in slot" → onUse(StudioJobStatus). (3) PMax wiring — StepImages slots + YT thumbnail Generate open StudioPanel (HiggsfieldGenerator modals removed from wizard; component itself kept for StudioPage until Phase B); StepText draft opens StudioPanel copy mode w/ host-injected drafter (onDraftCopy wraps existing draft-copy job+poll, preview-before-apply); post-crop preview: slot items render framed at Google's EXACT ratio (1:1/1.91:1/4:5) with object-cover center-crop + amber "will be cropped" flag when source ratio off >2% (no-silent-crop addendum, UI level). (4) Epic 11 P1 higgsfield scenes — `{type:"higgsfield", prompt, model, duration}` in storyboard renderer: services/higgsfield_scene.py (resolve via prompt-hash cache in ad_assets V18 column → zero-credit re-renders, else CLI submit_video 600s + download + record; mezzanine normalize 1080p/30fps/yuv420p + freeze-frame tail when speak > clip), premium_reel splices into xfade stitch (cap 2/render), pmax_video engine prefs (allow_higgsfield default OFF, model forced server-side, duration snapped) + draft-prompt extension + _clean_scenes degrade-to-broll, wizard "Allow up to 2 AI clips" checkbox + higgsfield SceneRow editing. Unit tests backend/tests/test_higgsfield_scene.py (7: clean-scenes cap/force/snap, normalize dims+pad, REAL ffmpeg splice w/ stubbed clips). Verified: tsc 0 new, vite build OK, backend restarted (launchd PID fresh), /api/health 200, /api/studio/models live (12 video/10 image), 1 live nano_banana image job completed end-to-end. NO Google Ads mutations, NO live video render | Epic 12 (12.1+12.4) + Epic 11 P1 — Phase A | backend/app/services/{model_catalog.py,higgsfield_scene.py} (new), backend/app/services/{higgsfield_client.py,premium_reel.py}, backend/app/routers/{studio.py,pmax_video.py}, backend/app/database.py (V18), backend/tests/test_higgsfield_scene.py (new), frontend/src/components/studio/{StudioPanel.tsx,useStudioJobs.ts} (new), frontend/src/components/video/ModelPicker.tsx (new), frontend/src/components/campaign/PMaxWizard.tsx, frontend/src/lib/api.ts |
| 2026-07-08 | Behavior guardrails WS2–WS5 (post-mortem: agent diagnosed from month-old data, never fetched the live landing page, ran a verbose 7-persona audit agreeing on a WRONG premise, cited possibly-inferred tracking IDs). WS2 verify-before-diagnose: new async `fetch_ad_landing_pages(account_id, campaign_id)` (reuses `_ads_svc.get_ads` + `page_fetcher.fetch`, dedupes final URLs, cap=3 `_MAX_LANDING_FETCH`, per-page 5s, never raises, emits per-URL status/title/h1/form-signal/tracking-token block) injected into BOTH `_get_campaign_data` paths (fast local-metrics early-return AND API fallback — the local path was the post-mortem failure) + global "VERIFY BEFORE YOU DIAGNOSE" guardrail in `system_parts` (survives future data/roles/*.md overrides) + tight versions on ppc_strategist/cro_specialist. WS3 freshness: `ROLE_NOTES_STALE_DAYS=7` + `role_notes_age_days(body)->(age,date)` parsing the `**Last updated:**` header; `build_campaign_context` prefixes `⚠️ STALE …RE-VERIFY` on stale/unparseable role-notes (never dropped) + global rule. WS4 ID integrity: no conversion/GTM/AW-/G-/label ID unless live-pulled THIS session + source-labelled — on analytics_analyst + gtm_specialist + global mirror. WS5 Team Audit retune: premise gate reuses WS2 (folds VERIFIED/UNVERIFIED PREMISE into `goal`, `phase:"verify"` SSE, never crashes) + ~200-word brevity cap + forced "What would change my conclusion:" line on every specialist task + both debate branches + premise-gated Director synthesis/plan. Conservative first-cuts flagged for Wassim: 7d window, 3-URL cap, label-not-block, 200-word cap, auto-fetch-on-every-campaign-chat perf. ADDITIVE ONLY — no persona prompt rewritten, no backend restart, no account mutation. Tests: 121 pass / 0 fail (stdlib unittest) incl. new tests/test_role_notes_freshness.py | NEW — unplanned (post-mortem guardrails) | backend/app/services/agent.py, backend/app/services/roles.py, backend/app/services/campaign_memory.py, backend/app/services/workflow_orchestrator.py, backend/tests/test_role_notes_freshness.py (new) |
| 2026-07-14 | **Video Director draft-timeout fix + campaign dropdown grouping** (2 surgical bugs). (1) TIMEOUT: `prompt_drafter._claude_one_shot` + `_stage1_decompose` gained defaulted `timeout_s` (default `_STAGE_TIMEOUT_S=45` UNCHANGED → image-drafting path stays 45s); video_director adds `_DRAFT_STAGE_TIMEOUT_S=150.0` threaded through `_draft_llm` (V3 concepts + V4 storyboard) AND `_decompose`→`_stage1_decompose` (V2), so all three video draft stages run at 150s while the shared image flow keeps 45s. Root cause: 45s cap tuned for small image prompts dead-ended video drafting once whole campaign-context block folded in (consult succeeds ~58s under its own 90s cap, next stage died at 45s). GRACEFUL: draft-stage `PromptDrafterError`/timeout now caught in `video_director_turn` → yields structured `{type:"turn_error",payload:{message:"The Director took too long drafting — this can happen with heavy campaign context. Retry.",retryable:true,stage:"draft-timeout"}}` + return (no auto-retry loop). RETRY: AIVideoStudio derives `retryableDraftTimeout` from events array, passes `onRetryDraft={onDraft}` (verbatim same POST /studio/video-projects/{id}/draft, same brief_source — idempotent per DB-row-as-truth) to DirectorDock which renders a "Retry draft" button in its error block (DESIGN.md accent tokens). (2) DROPDOWN: SetupRail campaign `<select>` flat map → native `<optgroup>` "Active (N)"/"Paused (N)" via `c.status === 'ENABLED'` (matches Sidebar), "No campaign" stays first+ungrouped, empty groups skipped, stable fetch order preserved. Kinetic studio correctly skipped (no campaign selector — hard-wires campaignId={null}). Tests 234→238 (uv run --with pytest, py3.12: timeout threading, 150s wiring, structured retryable event, +V4 storyboard path). tsc 0→0. Backend restart REQUIRED (not done — flagged to Wassim). | NEW — unplanned (2 verified bugs) | backend/app/services/{prompt_drafter,video_director}.py, backend/tests/test_video_director_turn.py, frontend/src/components/studio/AIVideoStudio.tsx, frontend/src/components/studio/aivideo/{DirectorDock,SetupRail}.tsx |
| 2026-06-11 | Studio redesign PHASE B (brief §5/§6): (1) AssetLibrary.tsx (new, story 12.2) — filter rail (type · source · campaign · model · created-date) + dense quiet grid, search across filename/script/prompt, hover metadata overlay (prompt/model/relative time), detail-expand modal (prompt, model, cost credits, aspect, dims, file size, campaign, absolute+relative created; Download/Compare/Delete + optional onUse for hosts), 2-up compare overlay, offset pagination 60/page "Load more" (paginate-past-100 requirement), states: loading skeletons / empty-with-CTAs / filtered-empty; decoupled — campaigns come in as a {id,name}[] prop. Server: /api/assets list minimally extended (model + since + offset params, q now matches prompt, response exposes V13 columns prompt/model/aspect_ratio/generation_cost_credits/status). (2) SoulCreator.tsx (new, story 12.3) — guided 3-step card (5-10 photo refs w/ thumbnail previews → name + soul-2/soul-cinematic → train) reusing S5 backend (POST /studio/soul/train, GET /studio/soul, 10s poll while training); status dots reuse plan-state pattern (training=studio-pulse accent, ready=success, failed=danger+reason+Train-again); ready cards get "Test generate" → StudioPanel preset {model:text2image_soul_v2, soulId}; copy promises only the backend-asserted 5-15min window — CLI has NO soul-training cost estimate so no credit numbers invented. (3) Hub slim-down (StudioPage rewrite) — Library/Souls/Presets tabs + ONE solid Create button opening StudioPanel (no campaign context; image/video tabs render in panel header via new onModeChange prop); presets flow via new StudioPanel `preset` prop (prompt+model+soulId, applied on reference change, model parks until catalog loads); onJobSettled prop → library invalidate; upload + Write script + New video kept as quiet actions; HiggsfieldGenerator.tsx + SoulCharactersPanel.tsx DELETED (no remaining importers — PMax verified clean). (4) Off-palette tint kill in touched surfaces — StudioPanel/StudioPage/MarketingPresetsPanel/ScriptGenerator/Header-Studio-button pink/violet/emerald/amber/red → DESIGN.md tokens (accent-soft/success/warning/danger + -soft). Verified: tsc clean, vite build OK, backend restarted (supervisor PID fresh, single listener), /api/health 200, / serves new bundle, assets list + model/since/offset/q filters live-tested, soul states verified via Playwright with 3 mocked DB rows (ready/training/failed — deleted after), test-generate preselect confirmed (panel selects = text2image_soul_v2 + mock soul_id), zero console errors. NO soul training run | Epic 12 (12.2+12.3) — Phase B | backend/app/routers/assets.py, frontend/src/components/studio/{AssetLibrary.tsx,SoulCreator.tsx} (new), frontend/src/components/studio/{StudioPage.tsx,StudioPanel.tsx,MarketingPresetsPanel.tsx,ScriptGenerator.tsx,useStudioJobs.ts}, frontend/src/components/studio/{HiggsfieldGenerator.tsx,SoulCharactersPanel.tsx} (deleted), frontend/src/components/layout/Header.tsx, frontend/src/lib/api.ts |
| 2026-07-04 | Account Director + Homepage v2 planning artifacts: PRD §8 Phase 1.6 added (problem / Wassim decision 2026-07-04 / feature table / non-goals incl. no-re-skin + chat-rail-stays-on-campaign-pages / success criteria) + Epic 13 with 8 stories (13.1 account-wide orchestrator mode w/ campaign-cap + cost-cap guardrails, 13.2 account report persistence V19 + latest-report read API + deterministic signals lane, 13.3 findings→approvable-actions contract via plans/scope-guard path, 13.4 weekly audit ritual auto lane, 13.5 homepage shell one-column/icon-rail/summoned-chat, 13.6 fix-list strip, 13.7 KPI cards + PoP metrics endpoint, 13.8 ranked campaigns + agent activity + Conversation Map to own page). Numbering note recorded: 11/12 reserved by Studio track. PLANNING ONLY — no code | Planning artifacts (Epic 13) | _bmad-output/planning-artifacts/{prd-v2,epics-v2}.md |
| 2026-07-04 | Story 13.1 account-wide Team Audit: `run_workflow(campaign_id=None)` → Director plans ACROSS ENABLED campaigns (roster via campaigns_repo V11, ranked by recent spend from campaign_daily_metrics, capped by new env setting `WORKFLOW_MAX_CAMPAIGNS`=5; excluded campaigns NAMED in deterministic scope footer + `account_scope` SSE event — no silent truncation; zero ENABLED campaigns → clean error). Specialists fan out per campaign through EXISTING `_run_group`/`_run_agent` (per-spec campaign_id override, falls back to group binding → per-campaign mode byte-identical; role notes/memory/CampaignScopeMiddleware stay campaign-bound per pass; group result sort now by seq — role_id keying collapsed when one role audits several campaigns). Account runs are analysis-only: tools FORCED `[]` on every pass (Director-granted tools stripped + logged); passes capped 3/campaign. Cross-campaign debate = one UNBOUND pass per distinct role (budget-cannibalization / keyword-collision / audience-overlap focus). Synthesis emits structured findings JSON — per finding: title, campaign_ids, evidence, dollar_impact_wk (null = explicit unquantified; strings like "~$120/wk" coerced), action_category (scheduler taxonomy, invalid→other), recommended_action — normalized server-side ($-desc sort, unquantified last, "Total recoverable $/wk" RECOMPUTED, never trusts LLM math) → `account_report` field on workflow_done + persisted `account_rollup` workflow_reports row (13.2 consumes; zero schema change). Unparseable synthesis degrades to prose-only report — run never fails. Phase 0 stays ONE batched sync_account; cost-cap + _MAX_PARALLEL semantics unchanged. `WORKFLOW_MAX_COST_USD` also materialized in Settings so the documented env knob actually binds (getattr was dead — pydantic ignores undeclared env vars). campaign_name-without-id stays on the legacy campaign-ish path (not account mode). 8 unit tests (stubbed agent stream + real temp SQLite via init_db, ZERO live LLM/Google calls): enumeration/cap/exclusion + ENABLED-only, env cap, findings normalization, campaign-mode regression pin (tools pass-through untouched), account E2E (fan-out binding, forced [], debate unbound, rollup contract, DB persistence), degrade paths. Full suite 18/18; backend NOT restarted (parallel build running — operator restarts at end) | Epic 13 / Story 13.1 | backend/app/services/workflow_orchestrator.py, backend/app/routers/workflows.py, backend/app/config.py, backend/tests/test_workflow_account_mode.py (new) |
| 2026-07-04 | Video Engine P2 — Soul talking segment + segment-timeline dispatcher v1 (research/video-engine-plan.md §6 P2). §1a RESOLVED: no talking/lipsync model verifiable (CLI 0.1.40 session expired at build — read-only `model list` needs auth; local help + curated catalog have no speak model) → plan's stated NON-LIPSYNC presenter fallback implemented: Soul still (text2image_soul_v2 + soul_id) → veo3 image-to-video motion pass (CLI `--image` auto-upload; param name = one constant if smoke test disagrees) → script TTS rides the existing per-scene VO bed (clip audio stripped by normalize -an = plan's duck/replace rule). NEW services/video_engine.py: VideoEngineRequest segments (storyboard|higgsfield|soul) COMPILED to one flat storyboard → existing premium_reel normalize+xfade pipeline (zero new ffmpeg logic; audio continuity by construction); enforce_caps (1 soul + P1's 2 higgsfield, first-N win), attach_soul_ids (ready-only, injectable fetch), resolve_soul_clip w/ prompt-hash cache in ad_assets (re-renders free), estimate_timeline_credits (null-tolerant summed live estimate). NEW routers/video_engine.py: POST /estimate + /render job+poll (in-memory; DB-row job state deferred to P3 — no V19 migration next to parallel Epic 13). premium_reel: type:"soul" prepare/render branches + soul cap. pmax_video: soul_character_id engine pref (default OFF), draft-prompt talking-intro block only when soul READY (server-resolved soul_id, never LLM's), _clean_scenes soul validation (≤1, forced first, id forced), render delegates to dispatcher only when a soul scene exists. FE: StudioPanel video mode "Add talking intro (Soul presenter)" toggle + ready-Soul select + script + summed-credit pill ("N+" when partial) + engine job poll → finished asset joins result tiles; PMaxWizard "Talking intro" Soul select by the AI-clips checkbox, soul SceneRow (look prompt + honest no-lip-sync copy), pre-render "AI scenes ≈ N credits" line. Tests backend/tests/test_video_engine.py (15: compile/caps/attach/clean/two-stage resolve w/ stubbed client/cache-hit zero-credit/REAL ffmpeg timeline splice 1920x1080). Verified: 15+10 unit tests green, import app.main clean, tsc error-set identical to baseline (pre-existing HEAD errors only, none in touched files), vite build OK. NO live Higgsfield render, NO backend restart (parallel build running) | NEW — unplanned (Epic 11 P2) | backend/app/services/{video_engine.py (new),premium_reel.py}, backend/app/routers/{video_engine.py (new),pmax_video.py}, backend/app/main.py, backend/tests/test_video_engine.py (new), frontend/src/lib/api.ts, frontend/src/components/studio/StudioPanel.tsx, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-07-04 | Story 13.2 account-report persistence + homepage read API + runner reliability fix (backend only). (A) RUNNER DECOUPLE — the real bug: `POST /api/workflows/run` used to drive the whole workflow INSIDE the StreamingResponse generator, so a client disconnect cancelled execution and orphaned the row as eternal "running" (2 live zombies: e898a108-…, f3b630b1-…). NEW services/workflow_runner.py: `start()` launches `run_workflow(...)` in a DETACHED `asyncio.create_task` (returns once the run_id is known from the first `workflow_start` event), publishing every event to an in-memory per-run `_RunHub` (replay buffer + subscriber queues); the SSE endpoint is now a VIEWER (`subscribe(run_id)` replays history then tails live) — closing it only unsubscribes, the task runs to completion. `POST /api/workflows/runs/{id}/stop` → `stop()` cancels the task cooperatively (orchestrator's `except Exception` doesn't catch CancelledError→BaseException, so the wrapper marks the row `stopped`); also flips a lingering `running` row when no live task. `sweep_zombies()` marks any `running` row with updated_at older than `WORKFLOW_MAX_RUNTIME_MINUTES`(20)×`WORKFLOW_STALE_MULTIPLIER`(2.0)=40min as `failed`/`stop_reason='stale'` — runs on startup (reaps BOTH known zombies — verified present in real DB @ V18) + periodic sweeper (`WORKFLOW_SWEEP_INTERVAL_MINUTES`=10). SSE event shape + per-campaign mode UNCHANGED. (B) PERSISTENCE — DB V19 `account_reports` (PK account_id, latest-wins UPSERT: run_id, findings_json, total_recoverable_wk, campaigns_audited/excluded counts, parse_ok, generated_at); orchestrator persists the normalized account_report via new services/account_report_store.py after the account_rollup row (best-effort, never fails the run). `GET /api/accounts/{id}/account-report` (new account_router) → latest report + staleness (age_minutes/hours + is_stale past `ACCOUNT_REPORT_STALE_HOURS`=24) + clean empty-but-valid zero-state (exists:false) when none — local SQLite only, O(1) lookup, zero Google/agent. Read model carries every field 13.3 needs (title/campaign_ids/dollar_impact_wk/action_category/recommended_action + source run_id). (C) FAST-SIGNALS — NEW services/fast_signals.py: deterministic always-fresh aggregator over LOCAL rows only (no LLM): pending_approvals (scheduled_plans awaiting_approval), budget_pacing (campaigns.budget_micros + campaign_daily_metrics rollup → avg daily ≥ budget×`FAST_SIGNAL_PACING_RATIO` → weekly-normalized projected overspend), wasted_spend (ENABLED campaign, window spend ≥ `FAST_SIGNAL_WASTE_MIN_SPEND` & 0 conv → weekly-normalized $), tracking_gap (full-window 0-conv spender → possible conversion-tracking gap, unquantified). ENABLED-only (PAUSED never flagged), $ impact only where honestly derivable (pending/tracking = null, never invented); merged into the account-report endpoint. 16 new unit tests (stdlib unittest, IsolatedAsyncioTestCase, real temp SQLite via init_db, scripted fake run_workflow — ZERO live calls): run survives simulated client disconnect + late-subscriber full replay, stop marks stopped, sweeper flips stale + leaves fresh + reaps the 2 named zombies, save/latest/overwrite/empty/staleness, fast-signals math (0-conv waste sum, pacing threshold, paused-excluded, pending approvals, tracking gap, total+count). Full suite 49/49 green; `import app.main` clean. Backend NOT restarted (operator restarts once at end — sweeper reaps both zombies + V19 applies on that boot) | Epic 13 / Story 13.2 | backend/app/database.py (V19), backend/app/config.py, backend/app/main.py, backend/app/routers/workflows.py, backend/app/services/workflow_orchestrator.py, backend/app/services/{workflow_runner.py,account_report_store.py,fast_signals.py} (new), backend/tests/test_workflow_runner_and_report.py (new) |
| 2026-07-04 | Story 13.3 findings→approvable-actions contract (backend only). NEW services/finding_actions.py turns each persisted account-report finding (13.2) + each fast-signal into a concrete proposed ACTION and routes the 3 fix-list buttons through the EXISTING plan/scope-guard path — NEVER a direct Google Ads write. `propose_action(item, source)` maps onto the scheduler taxonomy (no new taxonomy invented): actionable = {budget,bids,status,geo,negative_keyword,search_terms} AND ≥1 target campaign; gating via `scheduler.infer_mode` (budget/bids/status/geo→approval-gated, rest→auto); advisory-only = {audit,report,other} OR any actionable-category finding with NO campaign binding → surfaced info-only with `advisory_reason`, `actionable:false`, no mutation fabricated ("rewrite landing page" stays advisory). Each action carries dollar_impact_wk snapshot, campaign_ids (findings' list + signals' single id normalized), a human-readable diff_preview, and a STABLE `finding_key` = sha1(source|category|sorted campaign_ids|normalized title)[:20] — deliberately excludes $-impact + evidence wording so a DENY sticks across re-audits until the finding's IDENTITY changes. `approve(once=False|True)` creates a Scheduled Plan via the real `routers/plans.create_plan` (gate inferred, never overridden): auto lane fires immediately through the scope-guarded `scheduler.run_now` (→ stream_agent_response w/ campaign binding → CampaignScopeMiddleware); gated lane creates the plan but does NOT auto-fire (parks for `scheduler.approve_plan` sign-off — no silent money write); approve_once = one-time plan run_at=now through the same gates. `deny` UPSERTs a `denied` row (tombstone even if the finding is no longer live). `list_actions(account_id)` merges live findings+signals → money-ranked proposed actions annotated with decision state; denied suppressed by default (include_denied for audit), recoverable total counts only actionable non-denied w/ $. Endpoints on the existing account_router: GET /api/accounts/{id}/actions (fix list), POST /api/accounts/{id}/actions/{finding_key}/decide {decision: approve|approve_once|deny}. DB V20 `finding_actions` (PK account_id+finding_key: source,title,action_category,status,plan_id,dollar_impact_wk). 20 new unit tests (stdlib unittest, real temp SQLite via init_db, real create_plan against the schema, scheduler.run_now STUBBED so the auto lane is proven-routed w/o an agent — ZERO live LLM/Google/HTTP): mapping per category (actionable/advisory + gated/auto), no-campaign→advisory, signal id normalization, finding_key stability, approve auto creates+fires vs gated creates-no-fire, approve_once one-time+guarded, deny persist+suppress+survives-reaudit+changed-finding-resurfaces+tombstone, money-ranked list + advisory flags. Full suite 69/69 green; `import app.main` clean. Backend NOT restarted (operator restarts once at end — V20 applies on that boot) | Epic 13 / Story 13.3 | backend/app/database.py (V20), backend/app/routers/workflows.py, backend/app/services/finding_actions.py (new), backend/tests/test_finding_actions.py (new) |
| 2026-07-04 | Story 13.5 Homepage v2 shell (FRONTEND ONLY — no backend touched). Replaced the bulky AccountOverview (lifetime KPI grid + OutcomeDashboard + ConversationGraph + campaign grid) with a clean ONE-COLUMN command center (HomeV2). Header = account name + date-range picker (7d default, persisted to localStorage via appStore.homeRangeDays) + Create Campaign. Stacked mounts top→bottom: ① FixListStrip (calm designed zero-state "No open findings" — the ONLY section allowed a non-empty empty state; no faked $ figure; 13.6 fills it), ② KpiCards (4: Spend/Conversions/CPA/Conv rate from existing fetchAccountChart rolled up over the window vs the immediately-prior equal window → value + Δ% [shown ONLY when the prior window has a non-zero base — never faked] + recharts sparkline [drawn only for metrics with a real daily field: spend/conversions/cpa, NOT convRate]; CPA/Spend deltas inverted lower=green; 13.7 swaps in a dedicated PoP endpoint drop-in), ③ CampaignsRanked (compact ranked TABLE from existing fetchCampaigns, active-first then by spend, name/status/spend/conv, no bulk actions — 13.8 enriches), ④ AgentActivity (recent actions flattened from /accounts/:id/activity + Upcoming from fetchUpcomingPlans + last-5 threads with "View all"→Conversations; NO undo, NO OutcomeDashboard mount so the "0% (0/0)" artifact is impossible; trust line "Every write is reviewed. Every write is reversible." under the write surface). Zero-state discipline everywhere: empty section renders nothing. CHAT: removed the permanent right rail ON HOME ONLY (campaign/other pages unchanged) → new HomeChatDock = floating button (bottom-right) + ⌘K opening the EXISTING ChatPanel as an overlay drawer (CommandPalette ⌘K stands down on home via new disableHotkey prop; Header "K" still opens the palette manually). SIDEBAR: new isHome prop → icon rail (Layers button) with the existing campaign tree as a hover/click flyout, tree body extracted to a shared panelBody so there is ONE tree; other routes keep the full sidebar. Conversation Map REMOVED from home → new Conversations page (Header nav button + appStore.showConversations) hosting ConversationGraph unchanged. All styling via existing DESIGN.md OKLCH tokens (no new colors/fonts, no dark). Verified: bare `tsc --noEmit` = 0 (task gate — root solution-tsconfig is a no-op); `tsc -b` new-error diff vs baseline = EMPTY (repo has ~24 PRE-EXISTING tsc -b errors + an already-broken `npm run build`, none in touched files); `vite build` succeeds (3748 modules, dist shipped). Orphaned by this change (left in place, not deleted): OutcomeDashboard.tsx + CampaignActivityFeed.tsx (no longer mounted on home — seams for 13.7/13.8). NO backend files touched, backend NOT restarted | Epic 13 / Story 13.5 | frontend/src/components/dashboard/{HomeV2,KpiCards,CampaignsRanked,AgentActivity,FixListStrip,HomeDateRangePicker}.tsx (new), frontend/src/components/layout/HomeChatDock.tsx (new), frontend/src/pages/Conversations.tsx (new), frontend/src/stores/appStore.ts, frontend/src/App.tsx, frontend/src/components/layout/{ContentArea,Sidebar,Header}.tsx, frontend/src/components/CommandPalette.tsx |
| 2026-07-04 | Story 13.4 "Weekly account audit" Scheduled Plans ritual — AUTO lane (backend only). An account-scoped `audit` plan (campaign_id=None) is now the account-wide-audit RITUAL: when the scheduler fires it, `_fire` routes it through the new `_run_account_audit(plan)` → `workflow_runner.start(goal=…, account_id=plan.account, campaign_id=None, campaign_name=None, timeframe="weekly")` IN-PROCESS (13.2 runner, detached task — NOT a self-HTTP call), instead of the per-campaign `_run_agent` chat turn. The account-wide run is inherently analysis-only (Story 13.1 forces tools=[] in account mode — nothing to approve); on completion the orchestrator's EXISTING 13.2 persistence stores the latest account_report — the scheduler does NOT re-persist. `is_account_audit(plan)` gates the branch = `action_category=='audit'` AND no campaign_id AND no campaign_name (a campaign-scoped `audit` plan still uses `_run_agent`). Recurring re-arm via the existing `_complete()` recurring logic (weekly:mon:09:00 default → next Monday 09:00). AUTO-lane is inherent: `infer_mode('audit')` already returns "auto" (audit ∉ _APPROVAL_CATEGORIES) — never approval-gated. New seeder endpoint `POST /api/plans/account-audit {account_id, recurrence?, timezone?}` one-click enables the ritual; IDEMPOTENT — dup-guard returns the existing plan (`already_active:true`) when an active recurring account-audit already exists for the account (status != done), so weekly audits never stack; the plan is a normal scheduled_plans row → appears in the existing Plans listing + dashboard /upcoming unchanged. Module constants added: AUDIT_CATEGORY, WEEKLY_AUDIT_RECURRENCE, is_account_audit(). NO migration (V17 scheduled_plans schema already supports account-scope + recurring; V19/V20 untouched). 10 new unit tests (stdlib unittest, real temp SQLite via init_db, `workflow_runner.start` STUBBED so NO live workflow fires — ZERO LLM/Google/HTTP): dispatch calls runner with campaign_id=None + right account + weekly tf, `_fire` routes to runner NOT `_run_agent` (guarded with a boom-fake) + re-arms + records done run, campaign-scoped audit is NOT the ritual, seed idempotency (no dup, second call returns existing, done ritual doesn't block new seed), ritual surfaces in listing + upcoming, recurring re-arm → next Monday 09:00, auto-lane classification + is_account_audit predicate. Full suite 85→95 green; `import app.main` clean. Backend NOT restarted (operator restarts once at end) | Epic 13 / Story 13.4 | backend/app/services/scheduler.py, backend/app/routers/plans.py, backend/tests/test_account_audit_ritual.py (new) |
| 2026-07-04 | Homepage fix-list strip + inline approvals (FRONTEND ONLY). FixListStrip fleshed out from 13.5's calm placeholder into the live money-ranked command strip: header "Needs attention" + "Total recoverable: $X/wk" (server `total_recoverable_wk`, no faked figure) + staleness label ("audited Xh ago", amber when `is_stale`) + "Run again" that POSTs `/api/workflows/run {account_id, campaign_id:null}` account-wide and tails the SSE (inlined fetch+ReadableStream, same as WorkflowPanel) to know when it lands → refetch. Each finding = one compact table row (icon·title·campaign chip·$-impact right-aligned, "--" when unquantified); click = progressive-disclosure expand showing evidence + controls. Actionable rows: [Approve][Approve once][Deny] → POST `/actions/{finding_key}/decide` (per-row busy spinner + refetch + inline flash, matching PlansPanel idiom — app has NO toast system) + [Review in chat] (new `home-chat:open` CustomEvent → HomeChatDock opens drawer + pre-seeds via existing `chat:send`); gated rows (`requires_approval`) show a quiet "needs sign-off" hint (approve still parks the plan); advisory rows (`actionable:false`) show `advisory_reason` inline, NO buttons. Denied → hidden on refetch (contract has no un-deny/reset decision, so no fake Undo — spec's documented fallback). Zero-state (no actions / `report.exists:false`): keeps 13.5's calm card, enriched with staleness + a Run again button. Wired to REAL backend shapes (live-verified against running :8000: `/actions` keys + `/account-report?include_signals=false`); decision-state field is `status` (proposed/approved/approved_once/denied) NOT `decision`, gating via `mode`+`requires_approval` — followed real code. New api.ts types+fns (FixAction/FixActionsResponse/FixDecision/AccountReportMeta; fetchFixActions/decideFixAction/fetchAccountReportMeta). tsc -b: 0 NEW errors (34 pre-existing baseline unchanged); vite build green. em-dashes stripped from UI copy per DESIGN ban. Backend NOT touched, NOT restarted | Epic 13 / Story 13.6 | frontend/src/components/dashboard/FixListStrip.tsx, frontend/src/components/dashboard/HomeV2.tsx, frontend/src/components/layout/HomeChatDock.tsx, frontend/src/lib/api.ts |
| 2026-07-04 | Homepage campaigns-ranked + agent-activity change log + KPI rewire (FRONTEND ONLY — the last homepage story). (A) CampaignsRanked upgraded from 13.5's basic table: added CPA column + HONEST threshold-flag chips derived only from the campaigns payload — "No conversions" (ENABLED + real spend + 0 conv) and "High CPA" (CPA > 2× the account BLENDED CPA, and only when a real converting-campaign baseline exists; relative, never a fabricated absolute). NO trend-spark column (campaigns endpoint carries no per-day field → not faked). When ≥1 flagged: ONE bulk-action bar with per-row select ("Pick which") + "Pause N" routed through the EXISTING guarded plans path — one `createPlan({action_category:'status', mode:'approval', schedule_type:'once'})` per selected campaign (parks awaiting sign-off in Plans tab; NEVER a direct Google Ads mutate). Row click → existing `setSelectedCampaign` nav. (B) AgentActivity upgraded: added a Change-log block reading `GET /api/accounts/:id/outcomes` (recommendations baseline snapshots, outcome_tracker.py) — each executed recommendation with before→after "CPA $X → $Y" (improved/degraded tint) when measured, "measuring…" when the post-window hasn't landed (no fabricated delta). RENDERED READ-ONLY: verified NO revert/undo endpoint exists in backend routers (only skills version-rollback, unrelated) → no Undo affordance that would 404. Measured-summary line ("N measured · X improved") gated behind measured≥1 (Agent-Performance zero-state ban). Kept: /activity timeline, upcoming Scheduled Plans, last-5 recent threads ("View all" → Conversations). Trust copy under write surfaces. (C) KPI rewire: KpiCards now reads the purpose-built `GET /api/accounts/:id/metrics/overview?days={7/14/30}` (13.7 backend) instead of the manual two-fetch fetchAccountChart rollup — single fetch gives metrics.{spend,conversions,cpa,conv_rate}={value,prev_value,delta_pct} + per-day series; delta shown ONLY when delta_pct non-null (null=prior empty → no fabricated delta), value null → quiet dash not 0, conv_rate ×100 (wire is a fraction), sparkline from series, respects persisted home window. Added fetchMetricsOverview + fetchOutcomes (+ types) to api.ts. All shapes live-verified via curl against :8000 (account 7178239091). vite build exit 0; zero NEW tsc errors in KpiCards/CampaignsRanked/AgentActivity (34 pre-existing baseline unchanged; api.ts's lone AccountHealth-unused is a pre-existing dead import in HEAD, untouched by me). em-dashes kept out of UI copy per DESIGN ban. Backend NOT touched, NOT restarted | Epic 13 / Story 13.8 | frontend/src/components/dashboard/CampaignsRanked.tsx, frontend/src/components/dashboard/AgentActivity.tsx, frontend/src/components/dashboard/KpiCards.tsx, frontend/src/lib/api.ts |
| 2026-07-05 | MCP keyword-status tool (`update_ad_group_criterion_status`) — closes P0 audit gap: the agent could not pause/enable a keyword. A keyword IS an ad_group_criterion, so added a status-flip tool to AdGroupCriterionService (params: customer_id, ad_group_id, criterion_id, status ENABLED\|PAUSED, validate_only=False). Builds `AdGroupCriterionOperation.update` with resource_name `customers/{cid}/adGroupCriteria/{ad_group_id}~{criterion_id}`, `update_mask=["status"]`, status via `AdGroupCriterionStatusEnum` (v23) — same proven logic as operations.py `/keyword/status`. Registers AUTOMATICALLY: appended to the `tools` list returned by `create_ad_group_criterion_tools`, which `register_ad_group_criterion_tools` iterates → `mcp.tool()` on the already-mounted server (NO mcp_main.py edit). Returns real `serialize_proto_message(response)` dict (resource_name included), mirrors sibling GoogleAdsException/Exception handling + attribute-set build style. `validate_only` added (defaults off) per broader-codebase convention (`request.validate_only=...`); this criterion service had none prior — decision-beyond-spec. VERIFIED (no live mutation): `import app.main` clean; tool present in the registered list; validate_only op-construction smoke (resource_name/status/mask/validate_only all correct) passes with zero network. Backend NOT restarted | NEW — unplanned (P0 audit gap-fix) | backend/google_ads/services/ad_group/ad_group_criterion_service.py |
| 2026-07-05 | MCP ad-extension creators: sitelink/callout/structured-snippet/call assets — closes P0 audit gap: mcp_main advertised "create sitelinks, callouts, call extensions, structured snippets" but AssetService only made text/image/youtube-video assets, so those tools didn't exist. Added FOUR create tools to AssetService, each building the structured Asset via `AssetService.mutate_assets` and returning the real `serialize_proto_message(response)` dict (resource_name/id) — modeled exactly on `create_text_asset` (name-default, AssetOperation.create, AssetType stamp, GoogleAdsException/Exception handling). `create_sitelink_asset(customer_id, link_text, description1="", description2="", final_urls=[])` → `Asset.sitelink_asset` (SitelinkAsset); final_urls set on the Asset message (NOT the SitelinkAsset — descriptions optional, only set when non-empty so blanks stay unset). `create_callout_asset(customer_id, callout_text)` → `Asset.callout_asset`. `create_structured_snippet_asset(customer_id, header, values=[])` → `Asset.structured_snippet_asset` (header passed through, API validates it's a predefined header). `create_call_asset(customer_id, phone_number, country_code)` → `Asset.call_asset`. All types from `google.ads.googleads.v23.common.types.asset_types`; AssetType members SITELINK/CALLOUT/STRUCTURED_SNIPPET/CALL confirmed present in v23. Registers AUTOMATICALLY: appended to the `tools` list in `create_asset_tools`, which `register_asset_tools` iterates → `mcp.tool()` on the mounted asset_server (NO mcp_main.py edit). `validate_only` added to all four (defaults off) per broader-codebase convention (`request.validate_only=...`) — the create_text_asset template had none; decision-beyond-spec. SKIPPED the nice-to-have create-and-link helper: it would need CampaignAssetService cross-instantiation + two-step atomicity handling inside the asset file (out of the file boundary / added risk) — each tool's docstring instead points the operator to the existing campaign_asset/ad_group_asset link tools with field_type SITELINK/CALLOUT/STRUCTURED_SNIPPET/CALL. VERIFIED (no live mutation): `import app.main` clean; all 4 present in the registered tool list; 6 new stdlib-unittest tests (SDK client stubbed with a real MutateAssetsResponse → ZERO network/LLM/Google) assert each oneof field + AssetType + validate_only propagation + resource_name shape + customer-id formatting — all green; pre-existing test_pmax_resubmit (touches AssetService) still 3/3. Backend NOT restarted | NEW — unplanned (P0 audit gap-fix) | backend/google_ads/services/assets/asset_service.py, backend/tests/test_asset_extensions.py (new) |
| 2026-07-05 | MCP audit Phase 2: un-stub attach_shared_set + convert fake-success stubs (value-rule/data-link/batch) to honest errors + mcp_main description accuracy. THE SAFETY WIN: several tools returned success-shaped dicts ("Request processed — requires additional v20 type support") with NO API call, so the agent believed a mutation happened when nothing did. (1) UN-STUB `SharedSetService.attach_shared_set_to_campaigns` — a stray `raise NotImplementedError` sat directly ABOVE fully-working CampaignSharedSet mutate code; deleted the raise so the real path runs (builds one CampaignSharedSetOperation per campaign → single MutateCampaignSharedSetsRequest). Minor fix beyond spec: the live code discarded the mutate result and returned None (dead commented-out `return`) — captured `response` and `return serialize_proto_message(response)` so the tool yields a real resource_name (serialize_proto_message already imported). (2) HONEST-ERROR the fake-success stubs → `raise NotImplementedError(<what's unbuilt + UI/GAQL fallback>)` so the call visibly FAILS instead of lying: conversion_value_rule_service create + update (remove@214/list left as-is — already honest/real), data_link_service create + list, batch_job_service `add_operations_to_batch_job` (was building EMPTY MutateOperation() objects that ignored operations_data and SENDING them → would corrupt the batch job; now raises BEFORE any send). Removed 3 now-dead imports in batch_job_service (AddBatchJobOperationsRequest/Response, MutateOperation). (3) mcp_main.py capability-blurb accuracy: keyword mgmt now says "add, update, pause/enable status, and remove" (status tool landed); campaign bid modifiers de-overstated from "device, location, schedule, demographics" → "call/interaction-type only; device+location via campaign_criterion bid_modifier; ad-schedule NOT supported" (matches campaign_bid_modifier_service which only has create_interaction_type_bid_modifier); ad-extensions line left intact (now accurate). The neutralized stubs were NEVER advertised in the instructions blurb (verified), so nothing to retract there; `other` group epilog "batch jobs" kept (group genuinely holds working create/get/run/list batch tools). Did NOT touch ad_group_criterion_service.py / asset_service.py (sibling P0 work, done+verified). VERIFIED (no live mutation, no restart): `import app.main` + all 5 edited modules import clean; grep confirms zero "Request processed" strings remain in the 3 stub files + all raises in place; 2 new stdlib-unittest tests (`test_shared_set_attach.py`, get_sdk_client patched → fake CampaignSharedSetService, ZERO network) prove attach builds one op per campaign w/ correct campaign+shared_set resource names, single request, serialized return, dashed-id formatting — both green. Backend NOT restarted | NEW — unplanned (MCP audit Phase 2 gap-fix) | backend/google_ads/services/shared/shared_set_service.py, backend/google_ads/services/conversions/conversion_value_rule_service.py, backend/google_ads/services/data_import/data_link_service.py, backend/google_ads/services/data_import/batch_job_service.py, backend/google_ads/mcp_main.py, backend/tests/test_shared_set_attach.py (new) |
| 2026-07-05 | Rebuilt fail-closed dry-run harness (dry_run.py + validate_all_tools.py) — validates all mutate tools via forced validate_only at the SDK layer (monkeypatches GoogleAdsClient.get_service; forces validate_only on both request-object and customer_id/operations kwargs forms; FAIL-CLOSED → raises DryRunBlocked if a mutate can't be flagged, never sends it). Ran live vs account 7178239091: 314 tools discovered, 237 classified mutate, 24 PASS / 1 FAIL / 212 SKIP (1 SAFETY-quarantined). Safety post-check: 33 mutates forced validate_only, 1 blocked-unforced (mutate_batch_job — request proto has no validate_only field, correctly quarantined; billing_setup same family), ZERO unflagged mutates executed, account counts unchanged. All 5 new tools PASS (update_ad_group_criterion_status, create_sitelink/callout/structured_snippet/call asset). Real bug found: user_list_create_basic_user_list sends an empty BasicUserListInfo → FIELD_CANNOT_BE_CLEARED (tool exposes no way to supply the required rule/URL). No backend restart; read-only on all google_ads/ + app/. Re-run: `uv run python validate_all_tools.py`. Re-findable regression check | MCP hardening | backend/dry_run.py (new), backend/validate_all_tools.py (new), backend/data/tool_validation_report.json (new) |
| 2026-07-07 | Studio "finished video" BACKEND — generate a finished 15/30/60s video through Higgsfield by auto-planning N clips sized to the target. New `plan_scenes(target_seconds, model_id)` in model_catalog.py: N=ceil(target/max_clip), per-clip = model's MAX legal length via `clamp_duration` (enum snaps to top value, int caps at max) — minimizes clips/seams; verified exact (kling3_0 60s→4×15, veo3_1 60s→8×8, minimax_hailuo 60s→6×10, wan2_6 60s→4×15; None-duration veo3→1 native clip; unknown/non-video→[]; N capped at MAX_HIGGSFIELD_SCENES). RAISED `MAX_HIGGSFIELD_SCENES` 2→8 (60s worst case = 8×8s Veo) — enforce_caps + premium_reel re-check inherit the constant; PMax `_clean_scenes` deliberately KEPT at 2 (separate ad-product constraint, doesn't flow through finished-video). Threaded `target_seconds`/`model_id`/`prompt`/`voiceover_script` through VideoEngineRenderRequest → VideoEngineRequest → `generate_engine_video` (planner branch at top: builds N `engine:"higgsfield"` segments ONLY when trio set + segments empty; else byte-for-byte unchanged). VO: whole-script `voiceover_script` in planner mode turns OFF per-scene sync so premium_reel's single spanning-VO track sizes to the stitched duration (planner clips carry speak=""). `/estimate` gained a plan_scenes branch so the UI cost-gate sums per-clip credits pre-render. TESTED (stdlib unittest, ZERO live Higgsfield/Google): new tests/test_model_catalog.py (plan_scenes matrix) + planner-dispatch + VO-bed + cap tests added to test_video_engine.py — 36/36 target green, full suite 115/115. Existing storyboard/soul/prompt-hash paths intact. LIVE end-to-end smoke render PENDING `hf auth login` (CLI logged out). Backend NOT restarted | NEW — unplanned (Studio finished-video, builds on Epic 11 P1/P2) | backend/app/services/model_catalog.py, backend/app/services/video_engine.py, backend/app/services/higgsfield_scene.py, backend/app/routers/video_engine.py, backend/tests/test_model_catalog.py (new), backend/tests/test_video_engine.py |
| 2026-07-07 | Studio "finished video" FRONTEND — new "Single clip / Finished video" sub-mode in StudioPanel video flow (default 'single' so nothing regresses). Reuses the shared ModelPicker (kind=video, /api/studio/models catalog; +tiny Chinese/American/Soul origin label). Finished mode adds: 15/30/60 target-length control (→ target_seconds; per-clip Duration Tune hidden; client `≈ N clips` hint mirrors the planner math); aspect-ratio gating reuses existing modelAspects/effectiveAspect (driven by modelInfo.constraints.aspect_ratios → Hailuo/Grok never show 1:1); flexible AUDIO — "Music bed" toggle (→ music_filename via a picker modal reading /api/assets audio, styled calm-light) + "Voiceover" toggle (→ voiceover_script textarea + voice_id `<select>` from /api/video/voices); COST GATE — debounced videoEngineEstimate({target_seconds,model_id,prompt,aspect}) shows credits before render, Generate button reads "Generate finished video (≈ N cr)" as the explicit confirm (no silent burn). On confirm → videoEngineRender with the finished-mode fields, reuses the existing engineJobId poll + result tiles. api.ts: added optional `target_seconds`/`model_id`/`prompt`/`voiceover_script` to render body + `target_seconds`/`model_id`/`prompt` to estimate body (segments made optional; all optional → existing callers compile). Talking-intro gated to single mode (finished-video owns its own audio). GATES: touched files (StudioPanel.tsx, ModelPicker.tsx, api.ts) add ZERO new tsc errors (baseline unchanged — the lone api.ts AccountHealth-unused is pre-existing). Dev server NOT run | NEW — unplanned (Studio finished-video, builds on Epic 11 P1/P2) | frontend/src/components/studio/StudioPanel.tsx, frontend/src/lib/api.ts |
| 2026-06-11 | PMax wizard image slots: "From library" inline picker (existing ad_assets images, newest-first, filename filter, multi-select to slot max, aspect shown as guidance not gate — same local-UUID shape the orchestrator bridge resolves) + "Enhance (Visual Director)" in HiggsfieldGenerator slot mode (rough idea + wizard brief/businessName/finalUrl/slot → existing 2-stage PromptLift drafter via extract-brief context mode → 3-angle variant picker). Backend: extract-brief now accepts `context` as alternative to `url` (synthesizes page dict, no fetch) | Epic 8 | backend/app/routers/studio.py, frontend/src/lib/api.ts, frontend/src/components/studio/HiggsfieldGenerator.tsx, frontend/src/components/campaign/PMaxWizard.tsx |
| 2026-07-04 | Homepage KPI cards backend: period-over-period metrics endpoint `GET /api/accounts/{account_id}/metrics/overview?days=7` (mounted on existing account_router in workflows.py). Account-wide rollup of ENABLED campaigns (roster from `campaigns` table, matching fast_signals.py) from local `campaign_daily_metrics` only — zero Google Ads calls. Returns 4 KPIs (Spend/Conversions/CPA/Conv rate) each as {value, prev_value, delta_pct} for current vs prior equal window + per-day `series` for sparklines. Zero-state honest: empty account → nulls not zeros; CPA/conv_rate null when denominator 0; delta_pct null when prior empty/zero (no divide-by-zero, no fabricated deltas). Logic added as module-level fns in existing metrics_store.py. 16 new unittest tests (rollup math, ENABLED scoping, null-guards, delta honesty, window slicing, series shape, zero-state); full suite 69→85 green | Epic 13 / Story 13.7 (backend) | backend/app/services/metrics_store.py, backend/app/routers/workflows.py, backend/tests/test_metrics_overview.py |
| 2026-07-05 | Homepage tighten + active-campaigns-only default (FRONTEND ONLY — no backend touched, no restart). Fixes two operator complaints on the Epic 13 home: (1) "still too long" and (2) "a campaign paused a year ago still shows". TASK 1 (shorter): AgentActivity collapsed from FOUR stacked sub-blocks to TWO — merged the redundant "Change log" (before→after outcomes) + "Recent actions" (agent timeline) into ONE unified "Recent activity" list interleaved by timestamp, newest-first, capped at 5 rows; kept "Upcoming" as a slim strip capped at 3; REMOVED the "Recent threads" block entirely (it lives on the Conversations page) → replaced with a single quiet "View conversations →" link. Dropped the now-unused fetchConversations query + MessageSquare/Activity icon imports. HomeV2 vertical rhythm tightened: outer stack space-y-6→space-y-4, header mb-6→mb-4, page py-6→py-5. AgentActivity intra-section spacing space-y-5→space-y-3, row py-2.5→py-2, label mb-2→mb-1.5, trust-line mt-2→mt-1.5. CampaignsRanked row cap: default shows top 6 with a "View all N" disclosure (+ "Show less"). (FixListStrip already caps via server ranking; KPI row stays 4.) TASK 2 (active-only): CampaignsRanked now DEFAULTS to ENABLED-only via a new "Active N / All N" scope toggle (was showing all 108 with paused sorted after) — old PAUSED/REMOVED hidden until the operator clicks All; within ENABLED, zero-spend dormant campaigns sink below spenders (de-emphasized, NO fabricated "stopped" flag — the Campaign type carries no activity/date field). Sidebar: the "Active/Paused/All" filter tabs were already present and already default to ENABLED (StatusFilter default 'ENABLED', shared panelBody so the home flyout inherits them) — verified intact, not lost. Side-effect: fixed a PRE-EXISTING tsc error (Sidebar AccountNode used out-of-scope `clientAccountId`) by threading it as a prop → tsc -b total 34→33. GATES: touched files add ZERO tsc errors (grep AgentActivity|CampaignsRanked|Sidebar|HomeV2 over `tsc -b` = EMPTY); `vite build` exit 0 (3748 modules). All styling via existing DESIGN.md OKLCH tokens, one-column, no new colors, em-dashes kept out of UI prose. Backend NOT touched, NOT restarted | Epic 13 / Story 13.5 refinement | frontend/src/components/dashboard/AgentActivity.tsx, frontend/src/components/dashboard/CampaignsRanked.tsx, frontend/src/components/dashboard/HomeV2.tsx, frontend/src/components/layout/Sidebar.tsx |
| 2026-07-05 | Widened dry-run harness to full mutate coverage via real-resource harvest — SKIP→PASS. Extended fetch_real_ids() with ~28 new GAQL fetchers to harvest a real id/resource-name for 49 resource types from account 7178239091 (budgets, ad_groups, ads, bidding_strategies, shared_sets, conversion_actions, user_lists, experiments, asset_groups, campaign/ad_group/customer assets, campaign_criteria, bid_modifiers, audiences, drafts, recommendations, PMAX campaigns, geo/language constants, youtube assets, etc.). Added a HARVEST_TOOL_ARGS override table (keyed on FULL tool name — bare names collide across services) authoring exact minimal valid args (real ids + structured op-dicts + optional changeable fields for update-tools + real enum literals for device_type/ContentLabelType/etc.) for the previously-SKIPPED tools; generic ctx= fallback for tools whose .fn rejects ctx; tool-defect classifier (IndexError/Unknown-field/AttributeError on well-formed args → FAIL); account-state FAIL→SKIP downgrade; new `harvested_resources` report section. SAFETY UNCHANGED: every mutate still forced validate_only=True via dry_run.force_validate_only() (untouched); fail-closed quarantine intact. RESULT vs prior 24/1/212 → PASS 85, FAIL 9, SKIP 143 (9 real tool bugs surfaced: BAD_RESOURCE_ID field_type-enum, REQUIRED keyword_plan_network, IMMUTABLE_FIELD status-on-create, FIELD_CANNOT_BE_CLEARED, USER_LIST_MUTATE_NOT_SUPPORTED, 2× IndexError on empty validate_only response, Unknown-field CustomerNegativeCriterion.keyword, RESOURCE_NAME_MALFORMED). Safety re-verified: mutates_forced=125, blocked_unforced=1 (batch_job quarantine), safety_ok=true, post-run reads 153 campaigns/200 assets unchanged; 0 unflagged mutates executed. dry_run.py + google_ads/ + app/ untouched | NEW — unplanned (MCP hardening) | backend/validate_all_tools.py, backend/data/tool_validation_report.json |
| 2026-07-05 | Fixed 9 mutate-tool bugs from the dry-run backlog → harness 9 FAIL → 0 (90 PASS / 0 FAIL / 147 SKIP, safety_ok true, no regressions). 5 fix-to-work: keyword_plan_campaign sets required keyword_plan_network; custom_conversion_goal normalizes conversionActions RN + guards results[0]; custom_interest drops immutable status on CREATE; ad_group_criterion_label + keyword_plan_campaign guard empty results[0]. campaign_asset_remove: RN now uses field_type.name (→ SKIP: asset not linked = honest account-state). 3 honest-error for Google-unsupported: customer-level negative KEYWORDS (raise → campaign/shared-set), Similar Audiences deprecated, basic user-list needs rule modeling. import clean; dry_run.py untouched | MCP hardening | backend/google_ads/services/{campaign/campaign_asset,planning/keyword_plan,planning/keyword_plan_campaign,conversions/custom_conversion_goal,audiences/custom_interest,audiences/user_list,targeting/customer_negative_criterion,ad_group/ad_group_criterion_label}_service.py, backend/validate_all_tools.py |
| 2026-07-06 | Panama QIP · Oman+Jordan campaign artifact (client-facing markdown) with REAL Keyword Planner traffic. Pulled live avg-monthly-searches + competition + CPC bid ranges for all 121 catalog keywords (73 EN + 48 AR) via Google Ads API v23 KeywordPlanIdeaService.GenerateKeywordHistoricalMetrics, account 7178239091, geos Oman(2512)+Jordan(2400), langs English(1000)+Arabic(1019), network GOOGLE_SEARCH, last-12-mo — READ-ONLY (no plan resource, no mutate, no restart). Added 429-QPS retry/backoff (per-method throttle honors server "retry in Ns"). Data = genuinely low-volume niche as the research predicted: total addressable 950 searches/mo dedup (EN 680 / AR 270, Oman 460 / Jordan 490); only golden visa (50/50), citizenship by investment (20/30), الإقامة الذهبية (40/30), الفيزا الذهبية (10/20) carry volume; Panama-specific terms nearly all <10; only 4 kw returned CPC bids (low $0.28-1.19 avg $0.61, high $1.49-11.90 avg $5.13, golden visa Oman peaks $11.90). Artifact: exec summary + recommendations (2 campaigns→6 ad groups, Max Conversions, negatives, Oct-2026 $300K→$500K lever) + budget plan (honest English-first-both-countries call at $30/day: 5-12 clicks/day vs 4-way-split starvation 1.2-1.9/cell; scaled $75/$150 scenarios) + per-cluster traffic tables (Arabic script+translit+gloss, sorted by volume) + totals/top-10. Zero fabricated numbers (every figure from the live pull); ambiguous "<10 / no data" join-cells cleaned to unambiguous "<10". Delegated authoring to general-purpose subagent from ground-truthed processed JSON | NEW — unplanned (client deliverable) | data/panama-qip-oman-jordan-campaign-artifact.md, data/fetch_kwp_panama_qip.py, data/process_kwp_panama_qip.py, data/panama-qip-kwp-raw.json, data/panama-qip-kwp-processed.json |
| 2026-07-06 | Greece Golden Visa keyword research + report (Oman+Jordan, EN+AR) — real KWP pull (111 kw), processed JSON, strategy MD, client artifact MD, HTML+PDF report mirroring Panama QIP deliverables | NEW — unplanned (client deliverable, read-only KWP) | data/fetch_kwp_greece_gv.py, data/process_kwp_greece_gv.py, data/greece-gv-kwp-{raw,processed}.json, data/greece-gv-oman-jordan-{keyword-research,campaign-artifact}.md, data/greece-gv-report/{build_report.py,report.html}, data/greece-gv-oman-jordan-report.pdf |
| 2026-07-08 | WS1 (agent-quality-hardening-plan) — ship the ad final-URL update tool, closing the confirmed product gap where `final_urls` existed only on create/read paths so "switch the landing page" required a destructive delete+recreate (dropped RSA pins/history + minted a new ad id). NEW MCP tool `ad_update_ad_final_urls(customer_id, final_urls, ad_group_id?, ad_id?, ad_resource_name?)` — updates an existing ad's landing page(s) IN PLACE via `AdService.mutate_ads` + `AdOperation.update` + `update_mask=['final_urls']` on the shared `ad` resource (mirrors the proven 2026-07-06 in-place RSA-copy edit; preserves pins/history/ad-id, any ad type RSA/ETA). Accepts either full `ad_resource_name` OR `ad_group_id`+`ad_id` (builds `customers/{cid}/ads/{ad_id}`). URL validation: non-empty + http/https only. Registered in `AdService.create_ad_tools` tools.extend → mounts under the `ad` namespace in the `core` group of `google_ads/mcp_main.py` (verified in live MCP surface). Also added router endpoint `POST /api/operations/ad/final-urls` mirroring operations.py (schema `UpdateAdFinalUrlsRequest`, same validation + `_ensure_sdk()` + cache invalidate). Dry-run: added `ad_update_ad_final_urls` entry to `HARVEST_TOOL_ARGS` in validate_all_tools.py (harvested ad_group_id+ad_id, target `.../lp/`); classified as mutate → runs under fail-closed `force_validate_only()`. TESTS (no live account, no prod restart, no real mutation): isolated dry-run harness proved `mutate_ads` called with validate_only=True, update_mask=['final_urls'], same ad id (in-place), forced=1/blocked=0; 4 validation-guard cases (ftp URL, empty string, empty list, missing ids) all raise; SDK proto builds; MCP tool present; FastAPI app + mcp_main import clean; full existing suite 117 tests green | WS1 / NEW — unplanned (P0 from Panama QIP post-mortem) | backend/google_ads/services/ad_group/ad_service.py, backend/app/routers/operations.py, backend/validate_all_tools.py |
| 2026-07-12 | Epic 0 (chat-orchestration-v2-plan) — P0 stop/bleed hotfix, F7: (bug 1) a running agent's reply BLED into another campaign's chat window; (bug 2) Stop never actually stopped the agent. FRONTEND identity guards (ChatPanel.tsx): added `conversationIdRef` synced to `conversationId`; every async writer captures its own `convId` and bails on any `setMessages`/`setIsResponding` when `conversationIdRef.current !== convId` (`guardedSetMessages` helper for the send reader, `isCurrent()` for the reconnect reader, guarded interval writes for the `chat:display` poller); on campaign-switch AND conversation-switch effects, `abortControllerRef.current?.abort()` + `tearDownChatDisplayPoller()` (poller timers hoisted to refs) — kills all three bleed writers by construction. BACKEND stop hardening (agent.py): `subprocess.Popen(..., start_new_session=True)` so the CLI's MCP/uv/npx/Chrome children join its process group; `stop_agent` now `os.killpg(SIGTERM)`→wait(2)→`killpg(SIGKILL)` (graceful ProcessLookupError/OSError), reaching orphaned children; new module-level `_stop_requested: set[conversation_id]` set by `stop_agent`, consulted at top-of-segment + in the `should_continue` continue-decision (closes the between-segments relaunch race), cleared at the start of each fresh `stream_agent_response` run (so a later legit message isn't perma-blocked); `_running_procs` retyped `dict[str, set[Popen]]` (register-add / discard-this-proc / stop-kills-ALL) fixing the parallel-specialist last-writer-wins key collision (workflow_orchestrator passes the same conversation_id to N specialists) — stop now reaches every child; user-stopped detection re-keyed off `_stop_requested`. NO-RESURRECTION (ChatPanel `onStop`): also `setMessageQueue([])` + strip pending msgs so the queue-drain effect can't auto-fire a queued send and resume the just-killed session (resume semantics for explicit "continue" unchanged). SECOND-SEND-while-running (chat.py:358) deliberately NOT touched — explicitly out of Epic 0 scope (deferred to E1's turn runner; frontend already queues sends so UX isn't raw-broken). Surgical diffs, no v2 architecture / no new event protocol / no DB migration. TESTS: new `test_epic0_stop_hardening.py` — `stop_agent` process-group-kill (spawns real `sleep`, asserts group dead via `killpg(pgid,0)→ProcessLookupError`) + `_stop_requested` flag lifecycle trio (flag-no-proc, should_continue→False + isolation, fresh-run clear). Backend 125 tests green (121 baseline + 4 new), frontend `tsc` exit 0 — both gates re-verified independently. Delegated build to general-purpose subagent; Dam3oun-Google grounded the brief (verified all file:line anchors) + re-ran both gates + spot-checked every edit in-file. NOT restarted — launchd backend restart + frontend hard-refresh required to activate | Epic 0 (Story 0.1) / P0 hotfix from Panama QIP post-mortem F7 | backend/app/services/agent.py, backend/tests/test_epic0_stop_hardening.py (new), frontend/src/components/layout/ChatPanel.tsx |
| 2026-07-12 | Epic A (dashboard-freshness-clarity-plan, P0 "the data is real again") — killed the 3-month silent-stale sync. ROOT CAUSE: `sync_engine._sync_campaign_daily` INSERTed ghost columns (`id`/`metric_date`/`status`) that don't exist in `campaign_daily_metrics` (real cols `date`/`campaign_status`, no `id`) → every row raised `no such column`, swallowed by a bare `except: continue` → ZERO scheduled rows since April; the per-campaign×per-day full-account query burned ~3,300 API ops/run → the 429 "operations for basic access" that froze `sync_status` at 2026-04-16; `synced_at` restamped from cache made stale data LOOK fresh. **A1** — deleted `_sync_campaign_daily` + ghosts; rewrote `sync_account` to ONE READ-ONLY GAQL `search_stream` per account (new `GoogleAdsService.get_campaign_daily_metrics` — SELECT campaign.id/name/status/bidding_strategy_type + campaign_budget.amount_micros + segments.date + metrics.*, per campaign×date, no aggregation) upserting the ACTUAL columns; zero-rows for ENABLED-no-data days (checked-vs-never distinct); account-wide query auto-covers SDK-created campaigns (RC-7 fix); roster refreshed same pass; short transactions. **~3,300 ops/run → 2.** **A2** — new `sync_state` ledger (V21 migration: last_attempt_at/last_success_at/last_error/consecutive_failures/in_progress/data_through_date, PK(account_id,domain)) as the UI source of truth keyed on `data_through_date` NEVER `synced_at`; resilient scheduler — `.done()` restart fix (copied scheduler.py pattern), 60s watchdog + `config` heartbeat, `asyncio.wait_for(120)` timeout fence, exponential per-account backoff (1→2→4→cap60), account source switched conversations→`accounts_v2`, boot-sync; `/api/health` upgraded with per-account sync_state + heartbeat age; config METRICS_HOT_SYNC_MINUTES=60/HOT_WINDOW_DAYS=3/FULL_SYNC_LOOKBACK=30. **A3** — `services/freshness.py compute_freshness()` (fresh=data-through-yesterday AND success<90min / syncing=in_progress / stale / error); `freshness` envelope on `/metrics/overview` + `/account-report` (JSON body), `X-Data-Freshness` header on `/campaigns` (body is a bare array — response_model stays intact); shared `<FreshnessChip>` (light-theme DESIGN.md tokens: subtle "live · synced 42m ago" / pulse "syncing…" / amber "data through Jul 8 · [Sync now]" / error+tooltip) wired into KpiCards + CampaignsRanked + FixListStrip; `useSyncNow` hook POSTs the manual sync + invalidates metrics-overview/campaigns/campaigns-freshness/fix-actions/account-report-meta. **A4** — `maybe_kick_sync` self-heal on /metrics/overview + /account-report + /campaigns + campaign-open: single-flight (skip if in_progress) + 10-min stampede guard (skip if last_attempt<10m) + staleness (kick if data_through<yesterday or success aged>60m); fires via create_task, read NEVER blocked on Google; Team Audit Phase-0 pre-fetch now writes real rows (sync_account fixed). **A5** — cache honesty: `get_or_fetch(return_meta=True)` marks stale/circuit-open-serve (default path byte-identical, no caller breaks), open-circuit-no-cache returns explicit `(None, meta)` not silent `[]`; chart endpoint writes only on LIVE fetch (no cache re-stamp); `metrics_store.sync_daily_metrics` INSERT-OR-REPLACE→ON CONFLICT DO UPDATE (stops NULLing campaign_status/bidding_strategy/budget_micros — RC-5 roster corruption fix). READ-ONLY toward Google throughout (SELECT GAQL only). TESTS: new test_sync_writer.py (column-correct INSERT proven vs temp SQLite: rows written with real date/campaign_status, MAX(date)=yesterday, zero-rows for enabled-no-data, ledger data_through set) + test_freshness_envelope.py (state math) + test_self_heal.py (kick/skip decision: single-flight, stampede-guard, staleness) — 164 backend tests green (up from 121), frontend `tsc` exit 0, V21 verified on fresh temp DB. Delegated to 3 general-purpose subagents (backend core → self-heal → frontend, sequenced on dependency); Dam3oun-Google grounded briefs against file:line, re-ran both gates independently. FLAGGED: /campaigns freshness ships as HEADER not body (response_model constraint); campaign-open uses full-account hot-window (3-day scope) not per-campaign GAQL (avoids forking the writer). NOT restarted — launchd backend restart + first `POST /api/accounts/{id}/sync` (or app-open self-heal) required to activate; DB auto-migrates to V21 on boot. Epic B/C (clarity + push/home) NOT started (A strictly first per plan) | Epic A (A1-A5, dashboard-freshness-clarity-plan) / P0 | backend/app/services/sync_engine.py, backend/app/services/freshness.py (new), backend/app/services/google_ads.py, backend/app/services/cache.py, backend/app/services/metrics_store.py, backend/app/database.py (V21), backend/app/config.py, backend/app/routers/workflows.py, backend/app/routers/campaigns.py, backend/app/main.py, backend/app/services/workflow_orchestrator.py, backend/tests/test_sync_writer.py + test_freshness_envelope.py + test_self_heal.py (new), frontend/src/components/dashboard/FreshnessChip.tsx (new), frontend/src/hooks/useSyncNow.ts (new), frontend/src/lib/api.ts, frontend/src/components/dashboard/{KpiCards,CampaignsRanked,FixListStrip}.tsx |
| 2026-07-12 | Epic B clarity pass (dashboard-freshness-clarity-plan §3, "every number explains itself") — B1/B2/B3, frontend-only, decorating Epic A's now-honest freshness data. NEW shared primitive `InfoHover.tsx` — an 11px keyboard-focusable lucide-Info glyph that reveals a calm hover card explaining a number; built on the raw Radix tooltip primitives (NOT the ui/tooltip wrapper, whose dark bg-foreground chip + arrow reads too heavy in this light app) so the card wears DESIGN.md surface tokens (bg-card/border-border/shadow-elevated); the App-root <TooltipProvider> still supplies accessible open/close+focus. **B1 (KpiCards)** — replaced the single vanishing guard `if (cards.length===0 || !anyValue) return null` with THREE states keyed off `toFreshnessState(freshness.state)` (fresh/syncing=healthy, stale/error/absent=pipeline-problem): (a) empty account (cards.length===0) → render nothing (unchanged); (b) pipeline stale/error → render FULL cards + amber FreshnessChip so staleness is VISIBLE even when values are dashes (kills the "silently vanishes" RC-6 face); (c) healthy but no data in window (!anyValue) → header+chip plus ONE quiet line "No activity in this window" instead of 4 dud cards. Per-card InfoHover: honest definition + REAL window dates from data.window/prev_window (local formatThroughDate mirroring FreshnessChip, no TZ shift, falls back to "{N}d window" never invents) + "ENABLED campaigns only" + conversion-lag note "Google restates conversions for several days" on Conversions+CPA only. Brief §7 header comment amended (fake zeros still banned; pipeline-stale + healthy-quiet states may render text). **B2 (CampaignsRanked)** — section header gains quiet "· last {N}d"; Flag column ordering now flag→no-data→dash where a campaign with `costMicros===0 && conversions===0 && impressions===0` (helper `hasNoData`) shows a NEUTRAL `no data yet` chip (bg-secondary, NOT amber) + explanatory title instead of the $0/— dud (RC-7 UI face); InfoHover on the Flag COLUMN HEADER exposing both flag formulas in plain prose (existing per-chip warning titles kept). `new` chip FORK → DEFERRED (see below), NOT built. **B3 (FixListStrip)** — dual staleness folded into ONE honest label: when the audit's underlying metrics data is stale (`toFreshnessState` stale/error) AND a through-date exists, the "audited Xh ago" text becomes amber `audited Xh ago · on data through Jul 8` (+ InfoHover on the audit-fresh-but-data-stale danger) via a local formatThroughDate; FreshnessChip kept alongside. Signal rows (`a.source==='signal'`) gain a quiet `last 7d` context chip + fast-signals InfoHover. BOTH [Run again] buttons (zero-state + header) now `disabled={auditing || isSyncing}` so an audit can't fire on top of an in-flight metrics sync. **`new`-chip FORK DECISION: DEFERRED.** Surfacing it needs `campaigns.created_at` threaded schema→repo(2 SELECTs)→CampaignResponse→router→ApiCampaign→Campaign→mapCampaign (~7 spots). The column exists (stamped at first-INSERT, never updated — stable) but on a rebuilt/fresh-install DB every pre-existing campaign's created_at is recent → FALSE `new` chips on old campaigns. Conservative default (defer if unsure) + real false-positive risk → shipped `no data yet` only, no backend touch (schemas.py unchanged). GATES: `tsc --noEmit` 0 (baseline 0), `tsc -b` 33 (baseline 33), zero errors in any of the 4 touched files, `vite build` exit 0. Delegated B1/B2/B3 to 3 general-purpose subagents in parallel (one file each); Dam3oun-Google created InfoHover directly, grounded the briefs, made the fork call, re-ran all gates. NOT restarted — frontend hard-refresh to see it (backend untouched) | Epic B (B1/B2/B3, dashboard-freshness-clarity-plan) / P1 | frontend/src/components/dashboard/InfoHover.tsx (new), frontend/src/components/dashboard/KpiCards.tsx, frontend/src/components/dashboard/CampaignsRanked.tsx, frontend/src/components/dashboard/FixListStrip.tsx |
| 2026-07-12 | Dashboard v2.1 B4 live-truth header + pre-flight (dashboard-freshness-clarity-plan PART 2 / §3.5) — closes the "clean but not clear" gap: the campaign header now proves WHICH values are account-truth NOW. FRONTEND (A): new `LiveHeadChip.tsx` (green "live · 12s" from verified_at / amber "couldn't verify · showing data from {last_synced_at}" — never silent, DESIGN.md text-success/text-warning + status dot tokens, one quiet line); wired into `CampaignTabs.tsx` header via a `useQuery(['live-head', accountId, campaign.id])` polling `fetchCampaignLiveHead` (staleTime 55s / refetchInterval 60s = server TTL, refetchOnWindowFocus off). When state==='live' the header line PREFERS the LIVE campaign_type/bidding_strategy over the cached campaign object (fixes RC-8 "app shows Maximize Conversions when the account says Maximize Clicks"); unverified falls back to cached values (chip flags amber). BACKEND (B): new `scheduler._live_preread_suffix(plan)` — a READ-ONLY pre-flight live control-plane read (reaches Google via `GoogleAdsService().get_campaign_live_head` exactly as metrics_store does — direct instantiation, lazy import) injected into BOTH the approval-mode analysis instruction (_fire ~line 275-287) AND the approve_plan apply instruction (~line 347), so the diff/write is against LIVE status/bidding/budget not roster cache. Fully defensive: no campaign_id → empty suffix (no call); any failure/None → "Live pre-read unavailable" note, NEVER blocks the plan (try/except + logger.warning). Agent Layer-5 as-of header (B4 3rd bullet) CONFIRMED PRE-EXISTING (metrics_store._as_of_header at ~L200, already prepended in format_for_agent at ~L276 with try/except) — NOT duplicated. READ-ONLY toward Google throughout (SELECT GAQL only, no mutation). TESTS (C): new `test_live_head_preflight.py` (stdlib unittest, get_campaign_live_head mocked, nothing hits a live account) — 5 cases: live-read injects LIVE status/bidding/budget + "not cached data" steer; failure degrades to unavailable-note without blocking; None-result degrades; account-scoped plan skips the call; missing budget_micros renders "unknown" (no crash). GATES: backend 182→187 green; `tsc --noEmit` 0 errors; `tsc -b` 33 (= ceiling, all pre-existing, zero in touched files). api.ts/appStore.ts/App.tsx/formatters.ts NOT touched (consumed fetchCampaignLiveHead/LiveHead added by lead). NOT restarted — launchd backend restart needed to activate the scheduler pre-flight (.py change); frontend hard-refresh for the chip | Epic B (B4, dashboard-freshness-clarity-plan) / P1 | frontend/src/components/campaign/LiveHeadChip.tsx (new), frontend/src/components/campaign/CampaignTabs.tsx, backend/app/services/scheduler.py, backend/tests/test_live_head_preflight.py (new) |
| 2026-07-12 | Dashboard v2.1 C3 keyboard+rail-home + C4 skeletons+prefetch (dashboard-freshness-clarity-plan PART 4 items 2/3/4, §5 rows C3/C4) — frontend-only, effortless-home + no-blank-flash. **C3 keyboard (A)** NEW `useKeyboardNav.ts` — a single global keydown handler mounted ONCE high in the tree (App.tsx MainLayout, exactly one additive `useKeyboardNav()` call + one import). Gmail-style two-key chords: `g` sets a ref timestamp, a second key within 1000ms completes — `g h`→home (setSelectedCampaign(null)+all show* false, same move as Header's Home button), `g c`→last campaign (store.lastCampaignId via setSelectedCampaign, no-op if null), `g p`→Plans (opens lastCampaign if none selected, then setTimeout-dispatches the existing `plans:open-tab` CustomEvent that CampaignTabs listens for — Plans is a tab inside a campaign, mirrors AgentActivity's Upcoming-row idiom). `Esc` on a campaign page (selectedCampaignId set) → home. SAFETY (why single keys are unsafe → only g-chords+Esc bound): `isTypingOrModal()` guard bails on activeElement INPUT/TEXTAREA/SELECT/contenteditable OR any open `[role=dialog]`/`[role=alertdialog]`/open menu/`[cmdk-root]` (chat composer + palette never eat a keystroke); also bails on meta/ctrl/alt combos (leaves Cmd+K to the palette). Hook reads store via getState() so it never re-binds per render. **C3 palette (B)** CommandPalette.tsx — new "Navigate" group (Go home g h / Go to last campaign g c / Go to Plans g p, last two only when lastCampaignId exists) wired to the same nav logic; empty-text copy generalized campaigns→results. **C3 rail (C)** Sidebar.tsx isHome icon-rail — new top Home item (lucide Home) above the Campaigns/Layers flyout button, active-state aware (isHome → bg-accent-soft text-accent + aria-current=page), clicking calls a local goHome() (setSelectedCampaign(null)+all show* false); flyout campaign tree untouched. **C4 skeletons (D)** NEW HomeSkeleton.tsx (+ named FixListSkeleton/KpiCardsSkeleton/CampaignsRankedSkeleton) using the existing `studio-pulse` class (DESIGN.md §Reusable) + OKLCH tokens (bg-secondary/border-border/bg-card) — strip 88px card, KPI row 4×96px (2×2 below sm matching KpiCards grid), table 6×40px rows; no spinner, no new colours. **C4 prefetch+gate (E)** HomeV2.tsx (kept diffs in a clearly-commented C4 region, separate from the C2 agent's context-bar/continue-card/SSE region): a mount `useEffect` fires `queryClient.prefetchQuery` for the four home queries in parallel (fire-and-forget, non-blocking) with the EXACT keys/queryFns the sections use — `['metrics-overview',id,days]`→fetchMetricsOverview, `['campaigns',id,dateFrom,dateTo]`→fetchCampaigns (dated key via windowFor, shared with CampaignsRanked), `['fix-actions',id]`→fetchFixActions, `['account-report-meta',id]`→fetchAccountReportMeta — so caches are shared; plus a first-paint skeleton gate (`showSkeleton` = both primary reads metrics-overview + dated-campaigns still isLoading with no cache) rendering `<HomeSkeleton/>` in place of the section stack, so cold nav shows in-layout skeletons and warm nav (cache hot) renders instantly (<300ms budget). Small-window table horizontal-scroll: DEFERRED (CampaignsRanked's grid already reflows; no md-breakpoint scroll added — noted for a follow-up). DO-NOT-TOUCH honored: api.ts/appStore.ts/formatters.ts/AgentActivity/HomeChatDock/FreshnessChip/chat/backend all untouched; App.tsx exactly one additive line + import. GATES: `tsc --noEmit` 0 (baseline 0), `tsc -b` 33 (baseline 33), `vite build` exit 0; keyboard input-guard verified (bails on INPUT/TEXTAREA/contenteditable/open-modal). Files: useKeyboardNav.ts (new ~135L), HomeSkeleton.tsx (new ~110L), CommandPalette.tsx, Sidebar.tsx, HomeV2.tsx, App.tsx (+2L). NOT restarted — frontend hard-refresh to activate | Epic C (C3/C4, dashboard-freshness-clarity-plan) / P2 | frontend/src/hooks/useKeyboardNav.ts (new), frontend/src/components/dashboard/HomeSkeleton.tsx (new), frontend/src/components/CommandPalette.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/dashboard/HomeV2.tsx, frontend/src/App.tsx |
| 2026-07-12 | Dashboard v2.1 B5 account context bar + C1 SSE invalidation hook + C5 external-change UI (dashboard-freshness-clarity-plan §3.4, §3 context bar, §1.7 push, rows B5/C1/C5) — frontend-only, consumes lead-added api.ts (fetchExternalChanges/ExternalChange) + appStore (lastCampaignId). **C1 (A) NEW `useAccountEvents.ts`** — opens ONE `EventSource('/api/accounts/{id}/events')` while accountId set; defensive `JSON.parse` (malformed frame → no throw); on `sync_completed` invalidates metrics-overview/campaigns/campaigns-freshness/fix-actions/account-report-meta, on `external_change` invalidates external-changes/account-activity/campaigns — all by key-PREFIX predicate (`queryKey[0]===key && queryKey[1]===accountId`, matching useSyncNow so every windowed variant refreshes); ignores `connected`/unknown types (keepalive comment lines never reach onmessage); EventSource auto-reconnects (onerror swallowed), closed on unmount/accountId change. **B (mount)** HomeV2 calls `useAccountEvents(accountId)` (home-scoped v1). **B5 (C) context bar** — under the account name, quiet `text-subtle` line "Command center · CID {accountId}". CURRENCY/TZ FINDING: NOT available client-side — AccountV2 (types/index.ts) carries neither, and NO backend router queries Google's customer.currency_code/time_zone (grep confirmed). Conservative per brief: show CID only (the accountId IS the CID), gracefully OMIT currency+tz, fabricate nothing. **C2 support (D) "Continue where you left off →" card** — when store.lastCampaignId set (always true on home since no campaign selected), a quiet bordered button below the header calls setSelectedCampaign(lastCampaignId); resolves the name from the warm `['campaigns']` cache HomeV2 already fetches, falls back to "your last campaign"; renders nothing when lastCampaignId null (zero-state). **C5 (E) AgentActivity** — (1) new "Changed outside the app" sub-block via `useQuery(['external-changes',accountId], fetchExternalChanges)`, capped 5, only when rows exist; each row "{campaignName}: {Field} {before} → {after}" with friendly field labels (status→Status/bidding_strategy→Bidding/budget_micros→Budget) + budget micros→$ (÷1e6), calm bg-card/divide-border styling matching the Recent-activity block, click opens the campaign; folded into hasAnything so it can carry the section alone. (2) Timestamps — new `absoluteTime()` (toLocaleString) added as a `title=` hover on EVERY timestamp (Recent activity rows via row.sortKey, Upcoming via next_run_at/run_at, external-change via detected_at); relative text stays visible. Refactored `actionTime` to share a `parseTs` helper with absoluteTime. DESIGN.md: no em-dashes/colon-lists in UI copy, OKLCH tokens only (text-subtle/text-muted-foreground/bg-card/border-border), no new colours/fonts — compliant. DO-NOT-TOUCH honored: api.ts/appStore.ts/App.tsx/formatters.ts/Sidebar/CommandPalette/chat/backend untouched. GATES: `tsc --noEmit` 0 (baseline 0), `tsc -b` 33 unique (baseline 33, zero in touched files), `vite build` exit 0. NOTE: concurrent C3/C4 agent also edits HomeV2 (their prefetch/skeleton imports coexist with my C1/B5/C2 region — both compile). NOT restarted — frontend hard-refresh; backend SSE endpoint already live | Epic B/C (B5/C1/C5, dashboard-freshness-clarity-plan) / P1-P2 | frontend/src/hooks/useAccountEvents.ts (new), frontend/src/components/dashboard/HomeV2.tsx, frontend/src/components/dashboard/AgentActivity.tsx |
| 2026-07-12 | Dashboard v2.1 C2 home-as-default-route + shared foundation (dashboard-freshness-clarity-plan PART 4.1, B2 fallback-lie kill) — Dam3oun-Google direct (the shared/risky files the parallel subagents were kept OFF to avoid api.ts/appStore/App.tsx races). **C2 (the highest-priority home-access fix)** — killed the last-campaign localStorage RESTORE HIJACK: appStore.ts no longer initializes `selectedCampaignId` from the saved id (`selectedCampaignId: null` on boot); the persisted id lives on as NEW `lastCampaignId` MEMORY (does NOT drive the view) powering the home "Continue where you left off" card; `setSelectedCampaign` records lastCampaignId on select (keeps it on clear), `switchAccount` clears it (a different account's memory doesn't apply). App is now a REAL default route: App.tsx gains `/campaign/:campaignId` + a two-way URL<->store bridge in MainLayout (same shape as the existing /studio + /c/:id bridges) — URL→store selects on landing (deep-link shareable + refresh-safe) AND clears selection when the path is "/" (so browser-BACK from a campaign lands on the command center, not bounce back); store→URL pushes /campaign/:id on select and "/" when selection clears on a campaign route (the `g h`/rail-home case). Loop-safe by construction (each effect acts only on a genuine path/selection mismatch; scoped to the campaign<->home pair, never stomps /studio /c /setup). **Shared foundation consumed by all 4 B/C subagents (done FIRST to serialize api.ts):** api.ts — added `fetchCampaignLiveHead`/`LiveHead`/`LiveHeadFallback` (B4) + `fetchExternalChanges`/`ExternalChange` (C5) typed clients against the pre-built backend endpoints; killed the B2 bidding-fallback LIE in `mapCampaign` (`bidding_strategy || campaign_type` → `bidding_strategy || ''`); formatters.ts `formatBiddingStrategy` returns '—' for empty/UNSPECIFIED/UNKNOWN (never the channel type dressed as a strategy). AUDIT FINDING: a prior session had already landed ALL Epic-A pieces + the BACKEND of B4 (live-head endpoint + GAQL + as-of header), C1 (SSE hub + /events), C5 (external_change + roster-diff + /external-changes) with green tests — the B/C work was almost entirely the MISSING FRONTEND wiring + the scheduler pre-flight, so the fleet decorated existing truth rather than rebuilding. GATES (full integrated state, all 4 subagents + this): backend `unittest discover` 194 green (was 182; +B4 pre-flight +other discovered), `tsc --noEmit` 0 (baseline 0), `tsc -b` 33 unique (baseline 33, ZERO in any touched file), `vite build` exit 0. DO-NOT-TOUCH honored throughout: no chat/* /ChatPanel/agent/chat_runner/chat_orchestrator/workflow_orchestrator/chat.py/database.py-migration touched; V22 left for the concurrent Chat-v2 agent (B/C needed no schema — external_change self-creates its table lazily). READ-ONLY toward Google everywhere. NOT restarted — launchd backend restart needed for the scheduler pre-flight (.py); frontend hard-refresh for all UI. DEFERRALS: B2 `new` chip (created_at false-positive risk on rebuilt DBs), C5 change_event GAQL attribution (bonus, roster-diff works without it), small-window table horizontal-scroll, currency/tz in the context bar (not exposed client-side — would need a backend customer.currency_code/time_zone read) | Epic B+C (C2 + shared foundation, dashboard-freshness-clarity-plan) / P1-P2 | frontend/src/App.tsx, frontend/src/stores/appStore.ts, frontend/src/lib/api.ts, frontend/src/lib/formatters.ts |
| 2026-07-12 | Chat Orchestration v2 MVP — backend engine. `chat_orchestrator.py` (NEW): the §5 state machine as one async generator `run_turn()` (TRIAGE double-gate → RECALL → VERIFY → PLAN ≤3 specialists → DISPATCH via custom proc_key-threaded dispatch loop → RESOLVE conflict-detection → SYNTHESIZE Director-only voice → GATE claim-gate stub → WRITEBACK role-notes), wired as the ORCHESTRATED mode of the existing chat_runner turn path. `task_ledger.py` (NEW): read-only `recall()` view over workflow_reports(origin='chat')/scheduled_plan_runs/session_summaries/role_notes with §8.2 staleness matrix as single-source constants; every DB read guarded. Specialist outputs persist to workflow_reports origin='chat'; Director-only voice → messages. Budget $5/6-min DEGRADE-to-synthesize. All LLM via stream_agent_response (no SDK). Opt-in via `ChatMessageRequest.orchestrate: bool`. 7 new tests (mock-LLM transitions, triage routing, ≤MAX cap, degrade→DIRECT, ledger empty-DB + scoring, field-name contract); suite 187→194 green. Stubbed for MVP: S7 claim-gate (Epic 4), S8 session-summary/chronicle writeback | Epic 2 / stories 2.1-2.5 | backend/app/services/chat_orchestrator.py, backend/app/services/task_ledger.py, backend/app/routers/chat.py, backend/app/models/schemas.py, backend/tests/test_chat_orchestrator.py |
| 2026-07-12 | Chat Orchestration v2 MVP — frontend live-activity UI (Epic 3). Two-step orchestrated send: toggle ON → `startTurn` POST returns `{turn_id}` → `streamTurn` (shared parseSse over `/turns/{id}/stream?cursor`) accumulates v2 envelopes into per-turn state fed to the existing `OrchestrationLedger`; `final_chunk` streams the Director prose into the bubble, `final_done`/`turn_done`/`turn_stopped`/`turn_error` mark complete. ChatMessage renders the ledger when `message.turnId` is set (live events OR lazy `/turns/{id}/events` history replay). Stop affordances: turn-header stop → `stopTurn`, per-specialist stop → `stopTurnCall` (live only, hidden in replay). Per-conversation toggle persisted in localStorage `orchestrate:<convId>`, DEFAULT OFF = direct-mode path byte-identical + all Epic-0/1.6 identity guards preserved. tsc clean (EXIT=0, no new errors). Deferred: token-level specialist previews (story 1.4), conflict/decision/claim-gate rows (story 3.3 — render seams already present) | Epic 3 / stories 3.1/3.2/3.4 | frontend/src/lib/api.ts, frontend/src/components/chat/ChatInput.tsx, frontend/src/components/layout/ChatPanel.tsx, frontend/src/components/chat/ChatMessage.tsx |
| 2026-07-14 | AI Video Studio — 3 surgical honesty/UX changes (brief IS the plan). **C1 Draft sources (Brief / From campaign / From landing page):** additive `brief_source:{type:"text"|"campaign"|"landing_page", url?}` on `POST /video-projects/{id}/draft` (default omitted = legacy text). Router 400s: type=campaign w/ no campaign_id on the row, or type=landing_page w/ no url. `video_director_turn` threads brief_source (chat_runner.start forwards **params verbatim → run_fn — no refactor); JSON-round-trips so the turn normalizes dict-or-object defensively. type=campaign synthesizes the brief from campaign profile+pinned_facts+recent decisions (NOTE: campaign_memory has NO keyword loader — "top keywords" from the brief could not be injected without a new primitive; deferred as out-of-scope for a surgical change; helper UI copy still says "top keywords"). type=landing_page fetches via EXISTING page_fetcher.fetch(url) → injects title/h1/body_excerpt with an anchor-in-real-claims steer; fetch failure DEGRADES (yields a `verification`/page-unverified event, turn continues). Combinable (campaign on row + url passed → both feed context). Persisted via V24 migration `ALTER TABLE studio_video_projects ADD COLUMN brief_source TEXT` (schema const 23→24; written in the same row-update that stamps updated_at, no new write path; NULL for plain-text). Frontend: 3 segmented chips (copied Length-chip tokens) in SetupRail "Start with a brief" — From-campaign disabled until a campaign is selected (tooltip "Select a campaign first", no text required when active), From-landing-page url input (http(s) sanity), 400s surfaced as text-danger under the button. **C2 Honest render failures:** premium_reel `_classify_scene_error(exc)` → (code, msg[:300]) where HiggsfieldError.code∈{auth,cli,nsfw,run,shape} else "other"; per-scene skip event now carries `error_class` (additive key); all-fail branch emits STRUCTURED `{type:"error",stage:"error",message,scene_failures:[{scene_index,error_class,message}]}` then returns (no opaque RuntimeError clobbering the classification; auth-class present → message leads with auth). NEW cheap pre-flight `GET /api/studio/auth-status → {logged_in,error_class,message}` (reuses client.get_balance() truth-path — no separate account_status method exists; always 200, never throws). Frontend: auth-status called on Studio mount; logged-out DISABLES the Render button + shows warning-token banner with `higgsfield auth login` in a mono <code>; DirectorDock reads the new fields → auth→login banner, quota→"Insufficient Higgsfield credits.", else real message; partial skips → compact per-scene warning-soft chips (Scene N + error_class). Render mechanics/stitching untouched. **C3 Honest cost estimate:** additive nullable `error_class` on CostEstimateNullable (populated `e.code or "run"` in the except branch; success path + null-credits contract unchanged). Frontend defect was on the REAL path — StoryboardCanvas footer uses `videoEngineEstimate` (not /cost-estimate): added `costState idle|ok|failed`, `totalLabel=null` unless a real number → failed shows "Cost estimate unavailable" + button reads plain "Render video" (a literal 0 can no longer render from a failed/null estimate). Header credits pill already handled null ('—'), left as-is. GATES: backend 216→234 green (18 new: test_brief_source×9, test_render_error_classification×3, test_auth_status×6 + migration test V23→V24); `tsc --noEmit` 0, `tsc -b` 33 (baseline 33, zero in touched files); vite build exit 0. No Google Ads mutations, no live Higgsfield calls in tests (all mocked). DO-NOT-TOUCH honored (ChatPanel/chat.py/chat_runner-core/chat_orchestrator/dashboard untouched). Delegated: 1 Explore map → backend subagent (all backend + tests) → frontend subagent (all UI, against landed shapes); Dam3oun-Google grounded briefs at file:line + verified the cost zero-guard + auth-gate directly. NOT restarted — launchd backend does NOT hot-reload .py; manual uvicorn/launchctl restart required to activate; DB auto-migrates to V24 on boot; operator must run `higgsfield login` once for any real render/cost | AI Video Studio 3-change brief / NEW — unplanned (P1) | backend/app/routers/video_director.py, backend/app/services/video_director.py, backend/app/services/premium_reel.py, backend/app/routers/studio.py, backend/app/database.py (V24), backend/tests/test_brief_source.py + test_render_error_classification.py + test_auth_status.py (new) + test_studio_video_migration.py, frontend/src/lib/api.ts, frontend/src/components/studio/aivideo/types.ts, frontend/src/components/studio/aivideo/SetupRail.tsx, frontend/src/components/studio/AIVideoStudio.tsx, frontend/src/components/studio/aivideo/StoryboardCanvas.tsx, frontend/src/components/studio/aivideo/DirectorDock.tsx |
| 2026-07-14 | Three diagnosed frontend fixes. (1) P0 chat "thinks forever": appended `?stream=1` to the legacy `/conversations/{id}/message` POST in `sendMessage()` and in ChatPanel's live `actualSend` inline fetch — restores the pre-v2 legacy StreamingResponse contract (backend now defaults to JSON `{turn_id}` and only streams with the flag); turn-transport paths (startTurn/streamTurn/orchestrated) left untouched; verified end-to-end vs real backend :8000 (curl -N shows routing/text/done SSE frames with the flag, `{turn_id}` JSON without). (2) Studio loses-everything-on-refresh: post-create `navigate('/studio/ai-video/{id}',{replace})` in AIVideoStudio so URL is source-of-truth; existing mount-loader already rehydrates all persisted fields; added header "New video" reset button (bare URL + clear state). (3) Campaign dropdown grouping: VERDICT serving-stale — grouping code (`SetupRail.tsx` ENABLED vs Paused) is correct, live endpoint returns UPPERCASE `status` (5 ENABLED/105 PAUSED); the served `dist/` bundle predates the fix. Operator must `vite build` + hard-refresh. ALSO fixed a genuine pre-existing scope bug (ChatPanel catch referenced try-scoped `assistantMsgId`/`assistantText` → TS2304, hoisted to outer scope) that was blocking `tsc -b`/`vite build`. Remaining 31 `tsc -b` errors are pre-existing WIP (unused-decl + recharts types) in 16 unrelated files, not this task's scope. `tsc --noEmit` clean. | NEW — unplanned (bugfix batch) | frontend/src/lib/api.ts, frontend/src/components/layout/ChatPanel.tsx, frontend/src/components/studio/AIVideoStudio.tsx |
| 2026-07-14 | Chat Orchestration v2 — stories 3.3 + 1.4. **3.3 (frontend-only):** the backend already EMITS full `conflict` (`positions:[{call_id,stance}]`, `between`, `topic`) + `decision` (`ruling`, `rationale`, `conflict_id`) payloads (chat_orchestrator.py:706-767, unchanged) — this replaced the two 1-line stub renders in `OrchestrationLedger.tsx` (`Conflict: {topic}` in muted; bare `{ruling}`) with real, collapsed-by-default expandable rows: `ConflictLedgerRow` = Gavel + "Conflict: {topic}" in text-text + "N positions" + expand → compact stacked positions "{RoleName}: {stance}" (call_id→role resolved at BUILD time via the existing specialists Map → getAgentProfile, rows stay pure renderers); `DecisionLedgerRow` = the "verdict" moment, ruling in text-accent (the only colour lift, no filled card) + expand → "resolves: {topic}" (joined by conflict_id) + rationale. Reused the file's own Row/Indent/Caret/Dot atoms + useState collapse template; event order preserved. Types needed NO change (ConflictPayload.positions / DecisionPayload.rationale already present). claim_gate row left untouched (Epic 4). **1.4 (backend + frontend):** token-level specialist previews. Backend: new `AGENT_STREAM_PARTIAL_MESSAGES` env flag (default OFF) → splices `--include-partial-messages` into all THREE CLI cmd blocks + parses `stream_event`/content_block_delta into a new `text_delta` event (`_extract_stream_text_delta` + `_emit_assistant_blocks` seams in agent.py). DEDUP: with the flag ON `_emit_assistant_blocks` still accumulates `full_response_text` (persistence/findings JSON stay complete) but suppresses the whole-block `text` put — the deltas already streamed it, so no doubling; flag OFF = byte-identical old behavior. All `stream_agent_response` consumers treat `text_delta` like `text` (chat_orchestrator dispatch+synthesis, workflow_orchestrator, scheduler, video_director, pmax, plans, pmax_video); raw direct-mode client SSE relabels `text_delta`→`text` (chat.py, video.py) so v1 rendering is untouched. Frontend: `PREVIEW_TAIL_CHARS=600` tail-window cap on `textPreview` accumulation (`(prev+chunk).slice(-600)`) so token streaming can't grow memory unbounded; the CSS 3-line clamp stays the visual cap; ledger only ever sees `agent_progress`, never `text_delta`. GATES: backend `unittest discover` 256→264 (8 new: `test_partial_streaming` — extract-delta ×4 + emit-dedup ×4); `tsc --noEmit` 0 (baseline 0); `vite build` exit 0, dist/ rebuilt → new bundle `index-GcCwIag4.js` (was `index--VYBrpuK.js`), index.html references it, mtime fresh. No Google Ads mutations. BACKEND TOUCHED (.py) → flag defaults OFF so prod unaffected, but a manual launchd/uvicorn restart is needed before `AGENT_STREAM_PARTIAL_MESSAGES=true` can ever take effect. DO-NOT-TOUCH honored (claim_gate.py/provenance.py/dashboard/studio untouched). | Epic 3 / story 3.3 + Epic 1 / story 1.4 | backend/app/config.py, backend/app/services/agent.py, backend/app/services/chat_orchestrator.py, backend/app/services/workflow_orchestrator.py, backend/app/services/scheduler.py, backend/app/services/video_director.py, backend/app/routers/chat.py, backend/app/routers/video.py, backend/app/routers/plans.py, backend/app/routers/pmax.py, backend/app/routers/pmax_video.py, backend/tests/test_partial_streaming.py (new), frontend/src/components/chat/OrchestrationLedger.tsx |
| 2026-07-15 | cmo-agent E3.S2 (Google Ads MCP gap audit + expansion, read-only). Live-drove the `--groups reporting` MCP against Wassim's real account to validate the CMO's read surface. **BUG FOUND + FIXED:** `SearchService.execute_query` unconditionally set `request.page_size` (default 1000) → Google Ads API v23 rejects ALL such calls with `PAGE_SIZE_NOT_SUPPORTED` (fixed server-side page size of 10000). This broke the CMO's ONLY metrics path AND account-enumeration path (both ride `execute_query` GAQL) — and every other consumer that passed the default. Fix: never set `request.page_size`; param retained (default 0) + documented as ignored; SDK iterator auto-paginates. Post-fix, live-validated the canonical GAQL metrics report (`campaign.id/name` + `metrics.cost_micros/impressions/clicks/conversions/conversions_value`) on acct 7178239091 (Mercan Group Main): last-30d $12,342.49 / 37,097 impr / 2,883 clicks / 81 conv (`cost_micros/1e6` spend mapping confirmed; `conversions` is a float; `conversions_value=0.0` — account tracks count not value, ROAS genuinely unavailable → flagged not synthesized) + `customer_client` account-tree enumeration. **NEW READ TOOL:** `list_accessible_customers` (search sub-server, wire `search_list_accessible_customers`) — zero-arg `CustomerService.ListAccessibleCustomers` account-discovery bootstrap using OAuth creds only (no customer_id / login_customer_id) so the CMO discovers accounts without holding any Google-side identifier (NFR-4). Added to the read-only search service (NOT by mounting the write-capable `customer` sub-server — honors "never widen to write-capable"). Live-validated: returns 2 accessible customers; passes the `LANGAR_AGENT_TOOL_ALLOWLIST` fence (non-allowlisted tools stay BLOCKED). No write tools added; no mutation surface introduced. CMO-side follow-ups (contract re-pin sha 81f49d531075a64c→80c83229f96eb9d1, allowlist + contract additions) delivered as patches to the conductor. | cmo-agent E3.S2 (cross-repo, google side) | backend/google_ads/services/metadata/search_service.py |
